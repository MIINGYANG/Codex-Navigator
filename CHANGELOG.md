# 变更记录

## 3.3.0 — 2026-09-23

- 桌面三栏布局新增可拖动分隔线，独立调整会话栏与问题/事件详情栏宽度；悬停或聚焦高亮，拖动显示当前宽度。
- 支持双击或Enter恢复默认、方向键微调、Shift加速、Home/End调整至边界、Escape取消拖动；拖动期间暂停画布重排和自动重新定位。
- 浏览器保存宽度偏好，窗口缩小时自动保留至少480px画布，恢复大窗口后还原首选宽度；折叠、画布全屏及窄屏抽屉保持原交互。

## 3.2.1 — 2026-09-17

- 修复 Web 删除未检查分页历史依赖的问题：移到回收站前扫描活动与归档会话的元数据，阻止删除仍被其他会话引用的来源文件，提示依赖 ID；检查不完整时拒绝删除。
- 保留重命名使用官方元数据接口、删除再次确认和系统回收站恢复流程；删除提示补充相关分支的使用限制。
- 提供只读来源会话诊断脚本与恢复指南，区分改名和缺失来源文件的影响；升级不会自动恢复之前删除的会话。

## 3.2.0 — 2026-09-17

本版功能更新仅涉及收藏与跨会话问题定位；`/btw` 内容收录尚未实现。侧聊设计预览和接入验证不属于已交付功能。

- 收藏入口新增“全部 / 问题 / 会话”分类；未收藏父会话的问题也可直接查找，显示来源、项目路径和时间，支持文字 / 来源 / 路径搜索。
- 复用全历史异步索引汇总收藏问题，兼容已有收藏文件；跨会话跳转以稳定身份匹配完整快照，拒绝重复身份、回滚或替换后的错误位置。
- 索引中、读取上限、存储损坏或原记录缺失时给出明确反馈，不改写 Codex 源记录。

## 3.1.1 — 2026-09-17

- 修复同一次压缩的 `compacted` 与 `ContextCompaction` 完成通知因毫秒级时间差、间隔状态记录而重复计数；按同轮次、互补来源和活动边界识别镜像，保留多次真实事件。
- 同一连线的多条压缩记录聚合为一个“压缩记录 ×N”标记，可展开该段时间列表并逐条查看详情；单条记录仍直接打开详情。水平连线标记置于卡片上方并以细线指回原边，避免紧凑间隙遮挡。
- 补充镜像防误合并、增量图与摘要计数、分组列表状态和桌面/窄屏回归。

## 3.1.0 — 2026-09-17

- 在原问题画布增加纵向 / 横向、舒适 / 紧凑切换，保留原节点关系、选择与拖拽交互，支持响应式多列排列。
- 增加会话收藏与问题星标、收藏筛选 / 优先和高亮；收藏独立保存在 Navigator 数据目录，使用稳定身份、文件锁与原子写入，跨服务重启和端口保留。
- 根据会话内成功提交证据显示绿色标记、仓库 / 工作路径、分支、版本标签与哈希；后台索引提供会话提交摘要，不执行历史命令。
- 上下文压缩显示紫色连线标记和事件详情，区分明确记录的自动 / 手动与触发方式未知；末尾或关联不明的事件仍在会话事件列表可查。
- 增加有界事件解析、去重、rollback / reset、异步工具结果、收藏安全及真实浏览器合成数据验收；同步前端内嵌资源。
- 修复并发 SSE 轮询挤占普通 API 队列的问题；收藏文件锁在保存完成后显式释放，避免子进程短暂继承描述符造成假冲突。

## 3.0.0 — 2026-09-11

- Web 从纯只读扩展为显式会话管理：列表及当前会话新增重命名和删除入口，取消操作不改变数据。
- 重命名通过 Codex 官方 app-server 更新持久名称，写前核对会话 ID/路径、写后读回确认；列表、当前标题和搜索分组优先展示保存的名称，不启动模型对话。
- 删除确认后仅通过 Linux 系统回收站移动所选 rollout；保留名称记录，可在系统回收站恢复文件并重启 Navigator，不永久删除或清理 Codex 数据库。
- 管理接口沿用本机令牌并强制同源 Origin、JSON 与正文限制；拒绝外部路径，处理超时、失败和后台旧结果回灌。终端/浏览/doctor 保持只读。
- 管理依赖 PATH 中的 Codex CLI 和 Linux gio；其他平台删除会明确提示暂不支持。

## 2.1.0 — 2026-09-09

- 网页新增蓝色路线专属标签页图标，资源随程序内嵌，主入口与 `/trail/` 兼容地址均可用。
- 会话列表与当前会话直接显示完整项目路径，支持一键复制；长路径自动换行，剪贴板不可用时选中文本供手动复制。
- 当前路径来自会话记录，兼容 `--session` 直接打开列表外会话；路径缺失时明确提示未记录。

## 2.0.0 — 2026-09-09

- 统一网页入口：`codex-nav --web` 直接打开 Question Trail 问题画布，不再提供旧网页阅读页、旧脉络轨道及切换交互；终端版功能保持不变。
- `codex-trail` 保留为兼容网页命令，版本与 `codex-nav` 统一为 2.0.0；安装一次即可使用终端或网页。
- 网页新增跟随系统、浅色、深色主题，覆盖问题卡片、详情、搜索、工具栏、小地图和窄屏布局；保存用户偏好并响应系统主题变化。
- 更新 README 与 Agent 安装指南，提供任意目录安装/更新最新 GitHub 代码、持久 PATH、自检及升级后重启方式。
- 增加统一入口和主题回归验收，发布截图仅使用合成会话；设计规格与 UI 参考材料不进入发布提交。

## 1.4.0 — 2026-09-08

- 随包新增独立 `codex-trail` 1.0.0：蓝白 Question Trail 问题画布，默认打开最新会话；保留 `codex-nav` 终端和网页阅读入口。
- 问题卡片与真实顺序/持久化分支连接，支持缩放、拖拽、小地图、路径聚焦、详情、前后跳转、原始对话与跨会话本地搜索，不推断语义或展示隐藏思维链。
- SSE 增量通知与新问题提示保护历史视口；补齐响应式面板、加载/空态/解析降级状态与减少动效适配。
- 新增 `--codex-home`、默认端口 47321、`--no-open` 与只读 `doctor`；React/TypeScript 前端产物内嵌，安装和运行无需 Node 或外部服务。
- 增加图模型、CLI、API、前端与浏览器验收覆盖；同步安装指南及隐私、关系和资源预算边界。

## 1.3.0 — 2026-09-08

- 网页新增“问题脉络”视图，横向问题轨道与纵向记录步骤联动，支持点击原文、节点复制、最终回复和返回阅读页；按 v 切换视图。
- 明确区分记录顺序与因果/内部思维，展示筛选间隙、省略、未知记录与独立活动告警，不借助 AI 猜测关系。
- 复用问题/活动有界分页与会话请求隔离，暂停自动跟随并保护历史节点、文本选择和滚动位置；支持窄屏、键盘与减少动效偏好。
- 增加纯投影测试、静态模块 HTTP 安全覆盖与合成会话浏览器集成验收，不修改 Codex 文件，不新增运行时依赖。

## 1.2.0 — 2026-09-06

- 新增 `--web` 本地网页模式，采用用户选定的 B「专注阅读」设计；HTML/CSS/JavaScript 内嵌 binary，无需前端运行环境。
- 接入真实主会话、目录与 Prompt 搜索、活动分页、可靠最终回复定位、复制、实时更新与历史阅读保护；支持宽窄布局及按焦点键盘导航。
- 新增 `--port` / `--no-open`，保留 `--session` / `--cwd` / `--all` / `--no-watch` 和原有终端模式。
- 本机回环监听、随机访问令牌、Host/Origin 校验、安全内容渲染与有界缓存；继续只读 Codex，不引入云、登录、遥测或 AI API。
- 新增 HTTP 安全/增量集成与前端状态、浏览器交互验收。

## 1.1.2 — 2026-09-06

- 分离轮次生命周期与活动错误：✓ 正常完成、✕ 明确执行错误、⊘ 中断，独立 !N 显示活动错误记录数。
- 修复工具错误导致完成轮仍显示叉，以及晚到工具错误覆盖终止状态的问题；错误/中断不再计入工具错误数。
- 正文改为 TURN STATUS，帮助明确“完成不代表结果正确”，由用户按 f 查看最终回复判断。
- 补充生命周期、增量更新、宽窄渲染和终端回归；记录当前 Web 尚未实现及可复用的核心架构。

## 1.1.1 — 2026-09-06

- 默认启动始终进入 Session Picker，即使只有一个主会话也等待 Enter 确认。
- 启动、返回及刷新列表只显示 MAIN，隐藏子代理和未知来源；--all 仅扩展日期范围。
- 保留显式 --session ID/path 打开任意来源，不重定向父会话；更新空态说明和入口回归测试。

## 1.1.0 — 2026-09-06

- Prompt / 活动改为独立 16 / 48 MiB 滚动正文预算，保留最新输入与回复；旧正文省略会同步预览、搜索与增量更新。
- 会话显示 MAIN / SUBAGENT / UNKNOWN、代理和父会话标识、更新时间；自动选择优先唯一相关主会话，不误开子代理。
- 新增 `f` 定位当前轮可靠最终回复，支持镜像 phase 升级及宽窄重排，不打断历史浏览。
- 监控标签使用 WATCHING / STATIC，不将文件监控误报为代理执行状态。

## 1.0.1 — 2026-09-06

- 修复正文栏 g/G：按当前焦点跳转顶部/底部，宽窄布局切换后保持一致，不改变当前 Turn。
- 时间线保留 g 第一轮、G 最新有效轮；同步帮助与状态栏提示。
- 初始化本地 Git main 分支，分别保存 v1.0.0 基线与 v1.0.1 修复，以版本标签追踪迭代。

## 1.0.0 — 2026-09-06

- 独立只读 codex-nav binary：Session Picker、Prompt Timeline、Viewer 和键盘搜索。
- 兼容 Codex 0.153.2 实际结构及旧版事件，过滤内部上下文与 reasoning。
- 有界流式加载、消息镜像去重、显式/旧版 Turn、可靠状态、rollback 标记。
- CODEX_HOME、目录匹配、近期/全历史发现、索引辅助、doctor 和可选配置。
- 后台实时增量读取、历史选择保护、文件替换恢复、Unicode 响应式布局、剪贴板降级。
- 自动化解析/发现/状态/读取/worker/终端测试、合成 fixture、只读审计与性能示例。
