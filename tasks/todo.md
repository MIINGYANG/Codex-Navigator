# Codex Navigator v1.0 实施计划

## v2.0.0 — 统一 Web 入口与深色主题

用户要求替换旧网页阅读模式：保留 TUI，`codex-nav --web` 统一打开 Question Trail，`codex-trail` 作为兼容入口。不修改 Codex；本次授权验证后推送 GitHub main 与新版本标签，不强推或重写历史。设计文件与 UI 参考保留本地，不纳入发布。

视觉方案：保留蓝白卡片/曲线布局，深色使用画布 #0f172a、面板 #111827、卡片 #1e293b、边框 #334155、正文 #e2e8f0、强调 #60a5fa；原字体/空间层级不变。跟随系统默认，用户可切换浅色/深色并持久保存，画布、搜索、详情、小地图一致变色，无整页滤镜。

- [x] 统一 CLI/HTTP 静态入口，旧网页资源不再提供；覆盖 session、no-watch、原路径兼容与只读安全测试。
- [x] 实现三态主题、启动无闪白、本地持久化/系统变化、响应式与画布配色；增加自动测试。
- [x] README/Agent 指南同步双模式、安装更新；设计文件精确排除，保留本地副本。
- [x] fmt/clippy/前端格式lint/全量测试/release/浏览器浅深色与终端回归、隔离安装验收。
- [ ] 记录结果并提交/tag；核对远程后非强制推送 main 与新版本标签，验证远程提交。

### v2.0.0 验收 — 2026-09-09

179 项 Rust 测试、30 项当前前端测试全部通过（旧阅读页30项测试随旧界面退役，保留现画布23项并新增7项主题测试）；fmt、clippy全目标-D warnings、TypeScript strict、ESLint、Prettier、production bundle、双binary release通过。cargo package --offline --allow-dirty 完整打包并在独立打包源码中编译验证；隔离离线cargo install安装两命令为2.0.0，从/tmp调用版本/doctor通过，未覆盖用户全局安装。

浏览器1848/1000/390px完整回归通过，含浅/深/系统三态、刷新持久保存、不抢所选节点/viewport、完整问题与最终回复、跨会话搜索、千节点虚拟化、小地图/SSE/截断尾行、无外部资源/XSS与合成源字节校验；独立服务验证--session优先非最新目标、--no-watch等待用户刷新才读新增。终端六组交互/恢复回归通过。实际浏览器揭示ReactFlow自身及Background SVG都带默认底色，已用共享变量覆盖并加入SVG精确颜色断言，最终浅深截图经目视检查，保留于/tmp/codex-trail-qa-85uaAA，README只使用合成产品截图。

主入口为codex-nav和codex-nav --web，codex-trail保留同版本兼容，/trail/旧地址可用，旧reader资源404。设计规格/原参考图仍精确忽略；已跟踪设计文档/预览、旧reader源码和旧浏览器脚本仅停止Git跟踪，本地文件全部保留且可从历史恢复，不改写既有远程历史。远程main已核对为本地祖先，待本次提交后仅常规推送main和v2.0.0，不上传无关标签。

## Question Trail v1.0 / Navigator v1.4.0 — 蓝白问题画布

已完整阅读用户提供的新规格并检查参考图。新入口 `codex-trail` 使用 React Flow + TypeScript，复用 Rust 只读解析和安全本地服务；保留 `codex-nav` 终端与原阅读界面。画布只表达真实顺序及明确的持久化父子关系，不展示隐藏推理，不推断语义分支。默认浅蓝点阵、白色圆角卡片、蓝色选择边框，左侧约 250px 会话栏、右侧约 360px 详情、中央可拖拽缩放图；不复制截图的登录、收藏、回收站等无关功能。

- [x] 数据：验证本机 schema；确定性问题图、分支证据、跨会话搜索、增量 SSE；解析/图/API 自动测试。
- [x] 应用：独立 CLI、内嵌静态资源、默认最新会话、缺失目录诊断；原入口不回归。
- [x] 交互：React Flow 画布、节点/边 hover、选择/聚焦/邻域、搜索跳转、详情/原始 Turn、缩放/小地图/键盘、实时新增提示与历史视口保留。
- [x] 视觉：参考图蓝白密度与卡片样式，桌面/中屏/手机响应式及减少动效；真实浏览器截图对照并迭代。
- [x] 交付：README、版本与变更日志；fmt、clippy、全量测试、前端检查、release、浏览器与终端回归；记录验收，准备独立本地 commit/tag，不 push。

实现边界：推荐的 Node 服务改为复用现有 Rust 服务，减少重复解析、安全及部署逻辑；Node 仅构建时使用。无模型 API、遥测、云服务或 Codex 写入。无法确定分叉点时保留主线并明确解释；不把 fork 记录序号或子代理元数据当作父问题。

### Question Trail v1.0 验收 — 2026-09-08

175 项 Rust 单元/集成测试、53 项前端测试（旧30 + 图18 + 状态5）全部通过；fmt、clippy --all-targets -D warnings、Prettier、ESLint、TypeScript strict、前端 production bundle、双 binary release 和 git diff --check 通过。隔离目录离线 cargo install 成功，从 /tmp 直接调用两命令与 doctor 成功；未覆盖用户全局安装。交付 codex-nav 1.4.0 与 codex-trail 1.0.0。

Chrome 真正视口 1848 / 1000 / 390px 的画布交互全部通过，包括跨会话搜索、前后问题、hover/连线、拖动平移、缩放/fit/全屏、160×92 小地图点击、复制/原文/final、坏行空态、SSE 新增与历史 DOM/视口保留、截断用户行补全恰好一次、XSS/外部资源0、合成源文件字节校验。1000 问题主线首屏仅6个节点 DOM、100%缩放，首次可读约1.25–1.27秒，搜索至Q1000约0.29–0.32秒（含CDP等待，非冷启动保证）。API 合成106.59MiB/1000轮首批约24ms、完整262–276ms（缓存友好，不含浏览器绘制）。旧 Web 实时/手动 × 1280/390px、旧脉络两宽度、终端六组全部通过。

主代理已逐轮查看与参考图对比，修复初始字号/缩放、dagre左右顺序、移动/中屏抽屉遮住节点、DOMRect原型属性展开导致NaN、小地图内部SVG尺寸及点击定位。无分支主线采用两列蛇形及方向箭头保证6张卡可读，不伪造分支；大批live读取与初次loading分开，避免吞掉新增提示。最终截图 /tmp/codex-trail-qa-4ohYYk/，README 使用合成桌面图；仅截图时暂藏已验证的沉浸式翻译扩展浮层，恢复原样，不改应用内容或扩展配置。1672px窗口请求被窗口管理器调整为1848px，按实际宽度报告；非手机真机验收。

只读抽查最近24份本机session的6,683条记录，发现message/event/msg变体、forked_from_id、forked_from_ordinal_exclusive、parent_thread_id、root_turn_id；仅明确parent_turn_id且可唯一向前对应时建立父问题关系。真实网页冒烟发现46个主会话、画布/实时连接成功、外部资源0，不导出内容。新规格与参考原图留本地并精确忽略；测试窗口/服务已关闭，不修改Codex/真实session、不自动push。

## v1.3.0 — 网页问题脉络与交互过程

实现范围：保留阅读视图，增加同会话问题脉络。横向问题路径连接真实轮次，选中问题后纵向浏览已记录的助手消息、工具调用/输出、文件活动和通知，点击查看全文，最终回复独立定位。连线仅表示记录顺序，不推断因果、主题或未公开思维；分页/筛选间隙、省略与未知类型显式展示。不新增云服务、AI API 或 Codex 写入。

视觉方案已与现有 B 阅读室核对：纸白 #f8f7fc、白 #ffffff、深墨 #302a43、紫 #7156ab、浅紫 #ede7f7、告警金 #916a32；沿用中文衬线标题、无衬线正文、等宽序号。唯一重点是“问题轨道 → 选中问题 → 记录步骤”的连线与短促选择动画，尊重 reduced-motion。窄屏轨道横向滚动、详情纵向排布；键盘与可见按钮功能等价。

- [x] 实现并测试纯状态投影：顺序/间隙、记录类型、省略、空态及有界窗口。
- [x] 集成双视图、问题导航、步骤展开、最终回复、分页与稳定实时更新，保持现有阅读功能。
- [x] 合成数据浏览器验收宽窄布局、键盘、搜索/跨页、快速切换、活动窗口与只读隐私。
- [x] 更新 README/CHANGELOG 和 1.3.0 版本，通过 fmt、clippy、前端格式/lint、全量测试、release 构建，记录结果并准备本地 commit/tag，不 push。

### v1.3.0 验收 — 2026-09-08

158 项 Rust 单元/集成测试、30 项前端测试全部通过，cargo fmt、clippy --all-targets -D warnings、Prettier、ESLint、release build、git diff --check 通过。终端六组交互/恢复回归通过；旧 Web 在 1280/390px 的实时与手动刷新模式均通过，新脉络在两种宽度下通过 105 轮/64 活动窗口、搜索间隙、分页焦点、原文 g/G、v 往返、节点复制/最终回复、会话隔离、历史 DOM/内外滚动保留、XSS/无外部请求及源文件 hash 验收。

浏览器复现相关性搜索倒序造成错误连线，已通过可选 order=chronological 在服务端分页前排序修复，并测试默认 relevance 不变；同时修复跨页以旧选择为锚点、内层正文 g/G 和隐藏视图焦点问题。先验证新测试失败，再验证修复。主代理已查看实际桌面/窄屏截图，合成验收目录：/tmp/codex-nav-flow-qa-HwDMeN；旧 Web 目录 /tmp/codex-nav-web-qa-IZ8QGa、/tmp/codex-nav-web-qa-jko8LN。测试窗口与服务均关闭。

交付 target/release/codex-nav 1.3.0，未覆盖用户已安装的命令或运行中的服务。只读边界不变，不修改解析器/真实 Codex 文件，不加依赖、AI API 或持久标注。关系只表达真实记录顺序，不能替代语义因果图或恢复未记录思维；新功能通过独立本地提交与 v1.3.0 标签保存，不推送。

## C 版宣传片 — 补充侧边栏灵感

用户选 C 并要求提及灵感来自 GPT 侧边导航栏跳转历史。按 ChatGPT 侧边栏的历史导航使用体验表达，保留昨天/今天开场，替换重问场景为六秒灵感示意，总长 32 秒；不展示或抓取真实 ChatGPT 历史，不暗示官方关联。沿用 C 配色和字型，侧栏选中条目与正文之间的连接是唯一新增视觉重点。

- [x] 保存原 C 版及制作源码，更新灵感分镜、字幕、发布文案和对比页。
- [x] 仅重导出 C，核对新增场景、音画/字幕时长与旧版可访问性。
- [x] 记录验收并准备独立本地提交，不 push、不改应用版本。

### C 灵感版验收

新增六秒侧栏概念示意，表达用户提供的 ChatGPT 历史导航灵感；C 为 32 秒，保留原反差开场。原 30 秒版本及修改前源码在素材包 `qa/c_original_30s/`，对比页可直接打开旧版；A/B 分镜与成片未改。四项制作检查脚本全部通过，覆盖完整解码、32.022 秒容器时长、音频、七段字幕、入场回归、原稿保留、25 个链接与合成数据 hash。新增场景与最终解码画面人工检查通过，发布文案/旁白/README/对比页同步。未重跑浏览器交互检查或应用构建，未改应用版本、不上传。

## 三版竖屏宣传样片与素材包

面向小红书/抖音，以问题开场，制作 A 痛点反转、B 键盘爽感、C 轻松吐槽三版 9:16 样片。素材放仓库旁独立目录 codex-navigator-promo-20260906，不上传平台、不 push、不修改应用代码。真实界面仅接合成数据，不泄露真实会话；原创动效/音效与清晰字幕，无外部素材版权负担。

- [x] 确认三版脚本、风格与产品事实，准备合成会话和真实界面素材。
- [x] 输出三版可播放 MP4、封面、字幕/旁白稿、发布文案与可再编辑制作源文件。
- [x] 制作本地对比播放页，检查画面/字幕可读性、时长/分辨率/声音与素材隐私，展示效果。

### 宣传素材验收

仓库旁 `codex-navigator-promo-20260906/` 已交付 A 28 秒、B 24 秒、C 30 秒三版 1080×1920 / 30fps MP4，含中文字幕与原创节奏/音效；无人声，另附录制旁白稿。包含六张封面、SRT、平台发布文案、真实界面的合成数据截图、制作源文件、README 和三版对比页。

三版全片解码、视频/音频规格、字幕边界、本地链接、合成会话 hash、入场动画像素回归检查通过；18 张实际解码画面及关键场景抽检通过。浏览器验证三个视频实际播放推进与互斥暂停、选择按钮、1848px 桌面和 390px 手机尺寸窗口布局。修复入场残影与搜索/回复演示连续性后重新导出复验，旧稿保留在素材包 qa 中。未进行平台投放或手机真机验收，未使用真实用户会话，未改应用代码或版本，未重跑应用构建；不 push、不上传平台。详细记录在素材包 `qa/REVIEW.md`。

## 持久 PATH 配置与 Agent 安装交付

用户授权修改本机 Bash 配置，并更新 README 让 Agent 完成持久 PATH 配置。仅最小追加、保留私有备份，不重装程序、不改系统软件、不 push。

- [x] 备份 ~/.bashrc，添加带文件存在检查的 Cargo 环境加载，保留其他配置。
- [x] README 的一句话指令明确授权必要的用户级 PATH 配置；Agent 指南补充备份、幂等修改和新 shell 验收。
- [x] 从不含 Cargo 的初始 PATH 启动新交互式/登录 Bash，在源码外验证命令与版本，记录结果并准备本地提交。

### 持久 PATH 验收

~/.bashrc 已先备份到仓库外私有目录，再追加带存在检查的 Cargo 环境加载。bash -n 通过，程序化逐字对比确认仅增加预期块一次，原配置全部保留。以 /usr/local/bin:/usr/bin:/bin 为初始 PATH，从 /tmp 与用户主目录分别启动新交互式和登录 Bash，均直接解析到已安装的 codex-nav 1.2.0；Cargo 为 1.98.1。嵌套新 Bash 验证 Cargo bin 仅出现一次。

另在源码外、新 Bash 与独立空态数据路径下直接运行 codex-nav，主会话选择入口正常，q 退出成功并恢复终端。验收命令没有提前 source Cargo 环境。README/Agent 指南链接、代码围栏与 git diff --check 通过。此次没有改程序源码、版本或发布标签，未重跑全量构建/回归，不自动 push；用户已经打开的旧终端需重新打开才能应用新配置。

## 已授权历史清理 — 两份根目录文档

用户明确授权清理这两份文档的全部 Git 历史，并更新 GitHub main 与 6 个既有标签。只过滤指定路径，保留仓库外 bundle 和本地文件副本；不删除其他设计/开发文档，不永久删除文件，不清理旧对象或改写 Codex 数据。

- [x] 记录远程引用并验证仓库外完整历史备份，确认工作区无用户未提交改动。
- [x] 移除 README 引用、添加精确忽略规则，重写全部可达提交及标签；逐提交验证其他文件与提交元数据不变。
- [x] 验证本地文档副本不变、历史不再包含目标路径及内容对象、当前文档链接正确。
- [x] 显式逐引用 lease + atomic 推送 main 和 6 个标签，重新克隆验证远程清理；记录结果与恢复边界。

### 历史清理验收

仓库外保留两份完整 bundle（清理前、准备提交后）、两份 Markdown 副本、旧新提交映射和远程 lease 快照。12 个提交逐一验证：除两份文档对应的根 tree 条目和父/tree 引用外，其他文件对象、作者/提交者/时间/消息完全不变；6 个注释标签仅变更目标提交。README 链接检查通过，两份本地文件 SHA-256 与备份一致且已 ignore。

本机 Git 2.25.1 不支持显式 start/commit 指令，首次引用事务被拒绝且未修改任何引用；核对随 Git 安装的手册后，改用受支持的 --stdin 批量旧值校验事务成功。远程采用逐 ref 显式 force-with-lease 与 --atomic 更新 main 和 6 个标签，无额外分支更新；全新 SSH clone 的 12 个提交、6 个标签中均无目标路径及全部 7 个历史文档 blob，fsck 连通性通过。此次只改文档/ignore 与 Git 历史，不发布新功能版本、不重跑程序构建或交互测试。

清理范围是指定分支/标签的可达历史；不清除本地恢复记录、不执行 gc/prune，也不保证旧克隆、fork、托管缓存或隐藏引用自动清除。已有协作者应重新克隆，不能把旧历史合并回公开仓库。验收记录作为普通后续提交同步，不再次移动版本标签。

## 安装 FAQ 与用户文档入口整理

仅修改文档：补充旧 Cargo / PATH 排错，同步 Agent 环境检查；从 README 用户入口移除开发过程链接，保留文件与历史，不修改 ignore、不卸载软件、不自动 push。

- [x] README 补充工具链检查和 FAQ，更新真实仓库入口，精简“更多”链接。
- [x] Agent 指南明确先查现有工具链与 PATH，再决定安装或升级；记录用户反馈。
- [x] 验证排错命令、版本约束、链接与文档差异，记录验收并准备独立提交。

### FAQ 与入口整理验收

本机子 shell 执行 source ~/.cargo/env 后，Cargo 解析到 rustup 安装，Cargo/rustc 均为 1.98.1；最低版本与 Cargo.toml 的 1.88、锁文件 v4 一致。README 仓库地址与 origin 一致，相对链接、代码围栏及 git diff --check 通过。仅修改五份文档，不修改锁文件、软件安装、用户 shell、ignore 或历史；未重跑程序测试/构建，未 push。开发记录仅取消首页链接，仍可在仓库浏览，不宣称已隐藏。

## 安装说明 — 任意目录直接启动

仅澄清现有安装能力，不新增安装器、不修改用户 shell 或全局安装。

- [x] 验证已安装命令脱离源码目录运行，并核对资源是否内嵌。
- [x] README 与 Agent 指南明确 PATH、首次安装和日常启动的区别。
- [x] 检查文档、记录反馈与验证结果，准备独立文档提交。

### 任意目录启动说明验收

在 /tmp 中将此前临时安装的 bin 加入子进程 PATH，command -v、codex-nav --version / --help 均通过；版本为 1.2.0。源码确认 Web 资源 include_str! 内嵌。此检查仅验证脱离源码目录调用，不等同于用户新终端持久 PATH 验收；未修改用户 shell 或全局安装。两份文档的链接、代码围栏与 git diff --check 通过。本次无代码变更，不重跑全量测试、不构建新版本、不 push。

## README 修正 — 终端与网页并列入口

本次仅修正文档，不改程序、版本或标签。

- [x] README 首屏并列两种启动方式，安装指令不再默认只启动 Web。
- [x] Agent 指南补充终端启动/验收/退出，区分两种界面的操作与交付。
- [x] 核对 CLI 与源码、检查文档链接和差异，记录反馈与验收，准备本地文档提交。

### 双入口文档验收

两份指南的相对链接与代码围栏检查通过；启动参数与 release --help 一致，交互式终端要求及 Enter/Tab/f/q/Ctrl+C 与源码一致。3 项 CLI、4 项正文导航测试通过，git diff --check 通过。本次只改文档，未重跑全量测试、终端实机验收或 release 构建，不发布新版本、不 push。

## 用户 README — 一句话交给 Agent 安装自检

本次仅改文档，保留 v1.2.0 二进制与标签，不新增安装器、不发布远程仓库。README 面向使用者，详细步骤移到 Agent 安装指南；缺少公开仓库地址时以用户提供源码目录为入口。

- [x] 精简 README：一句话 Agent 指令、手动启动、核心使用和必要限制。
- [x] 提供可执行的 Agent 安装/测试指南，明确平台、权限、只读、空态和启动验收。
- [x] 检查相对链接及实际命令，记录反馈/验证，准备独立文档 commit，不 push。

### README 验收结果

README 与 Agent 指南的相对链接、代码围栏及 git diff --check 通过；参数与 release binary 的 --help / --version 一致。复跑 157 项 Rust 测试（含 HTTP 集成）、18 项前端测试及 release build 全部通过。本次 shell 未将 Cargo 加入 PATH，使用已安装 Cargo 的绝对路径完成验证，未改动系统配置；指南已覆盖依赖与 PATH 检查。此前本机临时目录安装及脱离源码运行检查通过，本次不重复全局安装、不新增浏览器交互验收。仅提交文档，保留 v1.2.0 与既有标签；公开下载入口仍需仓库所有者发布后提供。

## v1.2.0 — B 专注阅读 Web 正式版

用户已选择 B 并授权推进。实施约定见 docs/web-implementation.md；确认既有 CLI/解析器/只读 worker 后并行实施，最后集成验收，不再停在预览。

- [x] 内嵌资源与 --web / --port / --no-open，保留 TUI 及现有启动参数。
- [x] 实现本机令牌认证、来源校验、分页与有界缓存的只读 API。
- [x] B 主会话入口、搜索、正文/活动/最终回复、复制、历史保护与响应式键盘导航。
- [x] HTTP 安全/状态/增量测试、前端状态测试与真实浏览器宽窄验收。
- [x] fmt、clippy、全量 tests、release、终端回归；同步文档与知识记录。
- [x] 验证只读边界，核对独立 v1.2.0 commit/tag 内容，不 push。

### v1.2.0 验收结果

157 项 Rust 测试（含 11 个 Web 单测、13 个 HTTP 黑盒集成）、18 项前端单测全部通过。fmt/clippy/Prettier/ESLint、release build、六组终端回归通过；终端 harness 修正同尺寸误清屏与 resize 后发键竞争后连续通过。浏览器 1280/390px × 实时/手动刷新四组通过，覆盖 105 轮目录、64 条活动窗口、复制降级、请求失败自动恢复、历史 DOM/分页/滚动保护、精确新增提示与安全渲染。

真实本机 Picker 显示 14 个主会话，主会话 API 正常加载；另显式只读历史子会话 12 轮，文件 SHA-256 不变，不记录内容。已打开正式本机网页供使用。详细结果与边界见 docs/qa.md；交付为独立本地 v1.2.0 提交与版本标签，不执行 push。

## Web 设计预选 — 已选择 B，保留设计阶段记录

范围：只做三套可交互 HTML 预览及本地展示，不实现 --web、HTTP 会话 API 或真实会话接入；现有 binary 与版本不变。使用合成数据，保持主会话入口、生命周期/活动警告分离及最终回复定位语义。

- [x] 写明三套视觉 token、布局和交互范围，确认差异不是仅换色。
- [x] 制作 A 清晰工作台、B 专注阅读、C 紧凑控制台，以及统一切换展示页。
- [x] 浏览器验证会话选择、搜索、时间线、最终回复、折叠活动、宽窄布局和键盘；核对无外部资源/真实会话请求。
- [x] 记录预览验收、打开本地展示页并准备独立设计预览提交；不发布新 binary 版本。
- [x] 用户已选择 B，进入正式 Web 实施计划。

### 设计预览验收

四份 HTML 内联 JavaScript 语法与无外部依赖检查通过。三套样稿在约 1280 / 390px 外框宽度下各 18 项浏览器断言通过，覆盖默认会话选择、搜索空态、选择、f/g/G、折叠、帮助、返回与无横向溢出；A 的模拟追加另检查历史保护和无 final 提示。已查看桌面三方案和手机截图，修正 A 的按钮焦点快捷键及选中轮次可见性。

原有 132 tests、fmt、clippy 与 release build 通过；Rust 源码与 binary 版本未修改。预览服务只绑定 127.0.0.1:8877，仅提供 docs/design-preview。后台 QA tab 已关闭，按用户展示需求保留新开的预览窗口；未操作已有页面。正式 Web 开发等待用户选择，不创建发布标签。

## v1.1.2 — 轮次生命周期与活动警告分离

正常完成只描述执行生命周期，不判断答案正确。工具/命令错误保留 activity.errors，Timeline 独立显示 !N；显式轮次错误显示 ✕，中断显示 ⊘，完成显示 ✓。未知/未完成/rollback 语义保留。Web 仅评估当前架构，不新增服务或网络监听。

- [x] 补充失败后重试再完成、未结束活动错误、明确执行错误、中断、晚到工具记录与 rollback 的 parser/state 测试。
- [x] 分离状态判定及错误计数，更新 Timeline、正文摘要和帮助，确保不宣称答案正确。
- [x] 验证宽窄终端、f/g/G、完整测试及 release；同步使用文档、Web 支持边界、经验和版本。
- [x] 记录验收并准备本地 v1.1.2 commit/tag，不推送。

### v1.1.2 验收结果

旧实现先复现“活动失败后正常 completion 仍为 Failed”；修复后新增 6 项 parser/state 和 4 项 UI 回归通过。全量 132 tests、fmt、clippy 零 warning、release build、6 组真实终端验收通过。只读检查本机 12 份 rollout 的事件类型，不记录内容；合成终端测试文件 hash 不变。Web 仅完成架构评估与文档，未实现或启动网络服务。

## v1.1.1 — 默认主会话选择入口

按最新反馈修正入口：不传 --session 时始终进入 Picker，只列明确识别为 MAIN 的会话；子代理与 UNKNOWN 不进入列表。--all 仅扩展日期范围；显式 --session 仍可直接打开任意类型，不重定向父会话。保留 cwd 相关度与更新时间排序。

- [x] 补充主会话过滤、显式打开非主会话、单一主会话不自动打开和返回/刷新 Picker 回归。
- [x] 移除自动打开路径，统一启动与返回列表的主会话过滤，更新空态和 CLI 说明。
- [x] 同步文档、经验、版本；通过 fmt、clippy、全量 tests、release 和实际终端验收。
- [x] 记录验收结果并核对本地 v1.1.1 提交内容与标签命名，不执行 push。

### v1.1.1 验收结果

122 tests 全部通过，fmt、clippy 零 warning、release build 通过。旧 release 已由真实终端测试复现唯一 MAIN 自动打开，新版终端 5 组验收通过，覆盖启动/Enter/s/r/搜索/--all、非主会话空态、显式子会话 ID/path 和原有 f/g/G/终端恢复。所有合成文件 hash 不变，不修改 Codex 数据。交付版本为 1.1.1，以独立本地 commit/tag 保存。

已完整阅读产品规格及使用指南。执行采用规格指定的 Rust 独立 TUI，不修改 Codex，不包装 Codex PTY，不写入 CODEX_HOME；无需阶段性确认。

- [x] Phase 0：检查工具链与真实会话结构，建立 crate 和兼容性记录，构建成功。
- [x] Phase 1：实现有界流式解析、领域模型、去重、状态与 rollback；通过 parser 自动测试。
- [x] Phase 2：实现近期发现、索引辅助、目录排序、配置和 doctor；通过 discovery 自动测试。
- [x] Phase 3：实现 Picker、Timeline、Viewer、搜索、响应式布局、键盘与安全终端恢复；通过状态及渲染测试。
- [x] Phase 4：实现后台增量读取、文件替换恢复、实时跟随与历史选择保护；通过增量测试。
- [x] Phase 5：验证大文件、Unicode、损坏记录、剪贴板降级及真实本机会话。
- [x] Phase 6：完成 README、架构文档、LICENSE、CHANGELOG 和发布构建。
- [x] Phase 7：运行 fmt、clippy、全部测试、release 及终端集成验收，记录结果。

## 设计约定

- 流式读取使用 File::open 和有界缓冲；后台线程分批更新，TUI 不执行全文件阻塞解析。
- 正常文本保存在有总量上限的 normalized model；超大记录跳过并累计诊断。
- 兼容 event_msg、response_item、task_started/task_complete、item_completed PascalCase 类型；优先 content_item_kinds 识别用户输入。
- 搜索索引随 Turn 变更增量更新。session_index 仅补充标题，真实 rollout 为准。
- 所有 fixtures 人工合成，不复制真实 Prompt、工具输出或认证数据。
- 核实计划与规格一致后直接实施；只有真实阻塞才暂停。

## 验收结果

全部 7 个阶段已完成。91 项自动测试通过；fmt、clippy（零 warning）、release 构建和伪终端交互验收通过。真实历史文件 hash 保持一致，真实活动会话观察到增量追加，无 reset；没有向 Codex 写入任何数据。

发布文件：target/release/codex-nav（1.0.0）。文档：README.md、docs/architecture.md、docs/session-format-notes.md、docs/qa.md。详细测试与平台限制记录在 docs/qa.md。

## v1.0.1 — 本地版本历史与正文首尾跳转

需求已确认：g/G 按焦点生效；时间线继续选择第一轮/最新轮，正文跳到当前轮顶部/底部，宽窄布局切换后行为一致。建立本地 main 分支，以独立 commit 和版本标签保留迭代历史。

- [x] 初始化 Git，提交现有 v1.0.0 基线并建立标签。
- [x] 补充宽窄切换与正文 g/G 回归测试，再修复按键分发。
- [x] 更新帮助、使用文档、CHANGELOG 和版本号至 1.0.1。
- [x] 通过 fmt、clippy、全量测试、release 与终端交互验收。
- [x] 记录验收与经验，核对 v1.0.1 提交内容和版本标签命名。

### 本次验收结果

基线 commit e9fd58a，标签 v1.0.0。新增测试先在旧实现上复现 g/G 错误切换 Turn，修复后 4 个导航回归测试全部通过。全量 95 tests、fmt、clippy、release 构建和真实伪终端宽窄切换验收均通过。

正文 g/G 只改变当前正文滚动位置，保留所选 Turn 和新增消息提示；时间线 g/G 语义不变。版本号、帮助、README、使用指南、CHANGELOG 与本地迭代约定均已同步。交付通过独立修复 commit 和 v1.0.1 标签记录，提交历史可用 git log --graph --decorate --oneline --all 查看。

## v1.1.0 — 最新输入保护、会话辨认、最终回复定位

三个已授权需求作为同一可验收版本推进；只读取真实 Codex 文件，所有写入与测试数据留在 Navigator 仓库/测试目录。

- [x] 建立内存耗尽回归：历史输出不能挤占 Prompt；连续大量 Prompt 后最新输入仍可见、可搜索，内存保持有界。
- [x] 将保留文本拆成 Prompt 与活动两个滚动预算；淘汰旧正文时保留时间线摘要和明确省略提示，增量更新搜索。
- [x] 按真实 metadata 识别主会话、子会话、未知来源，显示父会话/代理标识及更新时间，排序优先相关主会话，监控状态不暗示模型正在工作。
- [x] 使用 f 定位当前轮最后一条可靠最终回复；标注 FINAL ANSWER，没有明确结果时提示，不猜测，不切换 Turn。
- [x] 覆盖 parser/discovery/state、宽窄渲染、增量 worker 和实际终端交互；验证真实本机会话的识别信息，不记录敏感内容。
- [x] 更新 README、CHANGELOG、版本及验收记录；通过 fmt、clippy、全量 tests、release build 后准备本地提交和 v1.1.0 标签。

设计边界：每条 Prompt 仍保留 256 KiB 上限，Prompt 总预算 16 MiB，活动总预算 48 MiB；超限时让位给新内容。更早的 Prompt 保留预览，历史全文省略必须有明确标识。最终回复定位只针对已保留且有可靠标记的消息。暂不加入全文分页、磁盘缓存或跨会话正文搜索。

### 本次验收结果

新增 26 项测试，全量 121 tests 通过；fmt、clippy 零 warning、release build、终端宽窄导航和恢复验收通过。内存测试覆盖 49 MiB 工具压力、17.5 MiB Prompt、48 MiB 精确预算边界、长中文回复 phase 升级、后台 dirty 淘汰同步。真实主/子会话身份正确，分别检测到 9 / 4 条最终回复，历史文件 hash 不变。详细结果见 docs/qa.md；交付为 codex-nav 1.1.0，本地独立提交与 v1.1.0 标签，不执行 push。
