# Codex Navigator v1.0 实施计划

## v3.2.1 — 防止删除分页分支的来源会话，排查恢复失败（2026-09-17）

用户报告另一设备 `/home/lmy` 中恢复会话时报 `invalid paginated history lineage ... missing source rollout`，并确认曾在 Web 删除会话。本轮核对重命名与删除影响，查找可恢复来源，修复管理流程缺少依赖检查的问题；不猜测或删除谱系字段。

- [x] 用隔离 Codex 合成分页源会话/分支，复现改名、来源移走、恢复原文件的行为。
- [x] 删除前检查仍存活的来源依赖，存在引用或无法可靠检查时拒绝移动，添加自动测试。
- [x] 准备只读诊断脚本、合成测试与可核对的恢复指南。
- [ ] 问题设备可访问且找到唯一匹配后，安全恢复原来源文件并核对；目前待目标设备诊断结果或连接方式。
- [x] 同步3.2.1、说明与验收记录，完成fmt/clippy/测试/release及管理回归，以独立本地提交/tag保存。

当前限制：本环境没有 `/home/lmy`，已向用户询问问题设备的连接方式或是否使用诊断脚本。所有开发验证使用合成文件；真实文件恢复必须按精确ID、回收站原路径与内容身份核对，禁止覆盖现有文件或编造依赖内容。

### 3.2.1 开发验收

只读诊断脚本14项合成测试通过，覆盖归档引用、回收站编码路径、同名不同身份、缺失/损坏/重复元数据、符号链接、读取预算与源文件哈希不变；不自动恢复或覆盖任何候选。

Codex 0.153.2 隔离实验：官方重命名后所有 rollout SHA256 不变且分支能恢复；移走来源重现完全相同的 -32600 错误；恢复原文件后恢复成功；来源位于 archived_sessions 也可恢复。只有 history_base.thread_id 为文件依赖，forked_from_id 独立存在不据此阻止删除，不能用去掉 history_base 修复真实文件。

246 项 Rust、46 项前端测试通过，fmt、全目标全特性 clippy、前端 lint/format/build、release build 通过。管理浏览器专项在1848/390px通过：归档分支阻止删除并显示依赖ID、被拒来源字节不变、取消后可继续管理；原有重命名同步/搜索/刷新、删除当前/其他/最后会话及系统回收站恢复回归通过。已目视检查保护提示，无溢出；截图与合成数据保留在 `/tmp/codex-management-1848-cAtV5z`、`/tmp/codex-management-390-MAJvJA`。终端交互未变，不重复终端QA。未安装全局命令、重启用户服务或推送远程；真实问题会话尚未恢复。

## v3.2.0 范围澄清与 GitHub 推送 — 2026-09-17

用户明确本版仅交付收藏改进，授权更新文档并推送。保留原3.2.0功能提交与版本标签，不将侧聊预览或可行性验证描述为已实现，不在本轮继续侧聊开发。

- [x] 同步 README、CHANGELOG、Agent指南及侧聊说明的功能范围。
- [x] 检查文档、版本标签与远程祖先关系，提交文档并非强制推送main/v3.2.0。
- [x] 读回远程引用核对，记录验收与交付。


### 本次同步验收

本轮仅修改文档，README本地链接和git diff检查通过；复用3.2.0已完成的236项Rust、46项前端、完整构建和桌面/手机浏览器验收。推送前远程main为a3f1efd，是本地祖先，远程v3.2.0不存在。非强制原子推送后读回main为4175be3dc76e550ebc024fe1e37f67f3e5717e57，v3.2.0解引用为e00db399d8a42910edd2c1835b6f252a7ff91996，标签仍指向已验收功能提交。同步记录另作main文档提交，不移动标签。本轮未修改应用代码或安装本地命令。

## v3.2.0 — 跨会话问题收藏（2026-09-17）

用户已批准侧聊分支方案并授权实现，同时要求收藏入口可找到未收藏会话中的收藏问题。保持原问题画布；收藏中区分会话/问题，问题显示来源与项目路径，跨会话按稳定身份精确定位。侧聊先以隔离合成数据验证官方事件通道，再据证据接入；不能把未接入或不完整数据宣称为完整保存。

- [x] 验证 Codex 0.153.2 临时分支事件、父问题身份与被动订阅；记录实际支持范围。
- [x] 实现独立于父会话收藏的全历史收藏问题目录、稳定定位及错误/缺失反馈。
- [ ] 后续事项（不属于3.2.0）：在用户确定接入方式后实现侧聊采集、存储与界面；本版尚无侧聊收录或手动导入功能。
- [x] 完成收藏功能的解析/状态/API测试、fmt/clippy/前端检查/全量测试、release、合成浏览器验收。
- [x] 同步收藏版本3.2.0、README/CHANGELOG、经验与知识记录，准备独立本地提交与新版本标签，不自动推送。

实施核对：遵守不修改 Codex 源码、不包装 PTY、不发送模型 turn；仅 Navigator 自有副本与收藏落盘。使用官方接口的隔离 PoC 不修改真实 Codex 配置或会话。先完成用户可审阅的实现和验证，自动采集确有外部能力限制时如实说明，不伪造成功。

侧聊接入待选择：隔离 PoC 证实旁观连接收不到正文，ephemeral 不能读取turns或resume订阅；透明RPC中转可作为候选，但需显式调整Codex启动方式。已通过异步问题请用户选择“专用启动入口自动留存 / 保持现有启动手动导入”，未收到回答前不实施依赖该选择的改动。证据与边界见 docs/side-chat-capture.md。收藏目录独立完成并验证，不因这一选择停下。

### 3.2.0 收藏目录验收

236 项 Rust 测试、46 项前端测试、fmt、clippy 全目标零警告、前端 lint/format/build 与 release build 通过；两个入口均为3.2.0。新增目录通过原v1收藏文件读取全历史，索引纳入路径/稳定身份/时间的内存预算，身份按需生成；目录不会根据收藏字符串打开任意路径。文件替换、回滚、重复身份与完整快照校验确保不误跳旧qN。

Chrome收藏专项1848/390px通过：父会话未收藏、近期范围外问题、来源项目/正文搜索、分类空态、跨会话精确定位、切换会话时迟到响应保护、取消收藏即时消失、手机收栏、缓存收藏遇源文件替换时不误跳复用q2，以及合成源字节不变。原画布功能1848/390px回归也通过，覆盖布局、压缩、提交、收藏持久化及刷新换端口。已目视检查收藏专项截图，材料为 `/tmp/codex-favorites-1848-vqFw6y`、`/tmp/codex-favorites-390-BqJEtx`、`/tmp/codex-features-1848-y485Ly`、`/tmp/codex-features-390-FxKPXz`。所有自建测试窗口与服务均关闭。终端交互未改，不重复终端QA。

最终另同步侧栏版本文本3.2并重建前端/release；这项显示文字变化沿用已通过的功能回归。README、Agent使用说明、CHANGELOG、版本及内嵌资源同步；不替换当前全局安装或用户运行服务，不自动push。侧聊仅完成可行性验证，正式采集依赖用户选择启动方式，未将预览或PoC包装成已实现功能。

## /btw 侧聊留存 — 可行性与 HTML 预览（2026-09-17）

本轮只评估并展示设计，正式采集功能等待用户确认。沿用已认可的问题画布；主问题旁显示侧聊数量，按需展开分支，在右栏独立阅读。所有记录均为合成，自动保存状态只作演示，不能暗示已接入另一个 Codex 进程。

- [x] 核实临时侧聊的数据来源、跨进程获取边界与实现成本。
- [x] 制作独立 HTML 预览：分支展开/收起、原问题定位、正文阅读、收藏/重命名/导出及保存异常状态。
- [x] 浏览器验证桌面与窄屏、键盘与无真实数据访问，展示本地预览并记录验收。

设计核对：保留深色点阵、原蓝色主线、蛇形节点、缩放与小地图；侧聊用青色 #67D5DB 和短分叉线，与收藏金、提交绿、压缩紫区分。画布 #0F172A、面板 #111B2D、卡片 #1E293B、正文 #E2E8F0、主线 #60A5FA。标题沿用系统中文无衬线14px，正文14px/1.8，身份/时间使用DejaVu Sans Mono。紧凑通过间距而非缩小字实现。分支身份与完整性状态分开，不能按最近时间猜测挂载位置；保存副本不冒充Codex可恢复线程。设计材料沿用本地忽略规则，不改生产源码/版本或运行服务。

### 侧聊预览验收与边界

预览地址 `http://127.0.0.1:8881/`，源码及构建在已忽略的 `docs/design-preview/btw-branches/`，本地展示服务 PID 2205613，仅提供 dist。保留专用展示页，未改动已有网页服务和二进制。

独立 Vite 构建、格式检查通过；Chrome 1848 / 1280 / 390px 每组 41 项，共 123 项交互断言通过。覆盖主线与分支拓扑、收起不丢正文、来源定位、收藏不移动视口、名称与画布同步、演示偏好落盘、完整问答导出、全文搜索/空态/收藏筛选、回收站确认与恢复、未接入/中断说明、布局、Enter/Escape、主题与无外部数据请求。另实测画布拖拽、缩放和小地图 SVG 不裁切。已查看桌面与窄屏截图；截图材料 `/tmp/btw-preview-{1848,1280,390}.png`。测试窗口已关闭，所有内容为合成，没有触发模型请求或访问真实会话。

可行性结论：现有文件监听无法获取未落盘的 /btw；本机 Codex 0.153.2 共享 app-server、remote/proxy 与 Hooks 是候选入口，但还没有证明被动监听可完整覆盖其他 CLI 的临时侧聊并给出准确父轮次。正式采集需独立隔离 PoC，不能把启动另一个 app-server、thread/read 或只记录最终回复等同于完整录制。预览明确未实现自动采集；持久副本不是 Codex resume 会话。仅设计与评估完成，正式实现等待用户确认；无版本升级、发布标签或自动推送。

## v3.1.1 推送与本地更新 — 2026-09-17

用户明确授权推送GitHub并更新本地。复用3.1.1既有233项Rust、44项前端及桌面/窄屏验收；核对远程引用后非强制推送main和新标签，安装已验证源码并重启已核实的本机旧网页服务。

- [x] 核对远程分支/标签与当前安装来源，推送并读回确认。
- [x] 更新本地两个命令，验证新终端路径/版本与隔离HTTP启动。
- [x] 重启已核实的旧网页服务，确认运行新版并记录结果。


### 推送与安装验收

远程main原为af7117d，是本地祖先，远程v3.1.1尚不存在；非强制原子推送后读回main和v3.1.1解引用均为196adbe2b9041516535f42ae1d4a58436040021b。发布标签保持在已验收功能提交，后续同步记录作为独立文档提交推送main。

确认原安装来自本仓库GitHub的3.1.0；cargo install --path . --locked --offline --force成功替换~/.cargo/bin/codex-nav和codex-trail。两安装文件哈希与已验收release一致。以不含Cargo目录的初始PATH启动新交互Bash、在/tmp验证两个入口均解析到安装位置且版本3.1.1，无需修改shell配置。

隔离合成HTTP验收通过：未认证403、认证info版本3.1.1、页面/内嵌JS/CSS返回200，压缩镜像只保留1条，源文件字节不变。材料保留于`/tmp/codex-nav-install-3.1.1-bdljbtxf`。复用上一轮233项Rust、44项前端与浏览器完整回归，不重复构建测试。

精确核实并用SIGINT停止旧网页PID1719048，保留原工作目录和相关环境，在原8765端口启动新版后台服务PID1857780；认证info验证3.1.1，启动命令按默认行为请求打开新浏览器页面。停止命令：`kill -INT 1857780`。含启动令牌的日志仅保存在用户私有~/.local/state/codex-nav目录，不写入仓库；旧网页需使用新启动链接。

## v3.1.1 — 压缩重复计数与连线标记堆叠

用户在另一设备及当前设备均反馈同一连线出现多个压缩标记。本轮先诊断所指会话的最小事件元数据（不改源记录），用合成数据复现；分别处理可靠去重与同边聚合，不能通过隐藏事件掩盖计数问题。

- [x] 核对截图对应压缩记录的类型、时间、ID与轮次，确定重复原因并制作合成回归。
- [x] 修复有证据的镜像去重；同边多个事件聚合为一个标记，展开保留每条记录与详情。
- [x] 完成解析/状态测试、前端检查、release及桌面/窄屏合成浏览器验收。
- [x] 同步3.1.1版本、README、CHANGELOG、经验/知识记录，本地独立commit/tag；不自动push。


### 根因核对

当前设备截图对应的一组记录中，持久compacted记录与ContextCompaction完成通知相差19ms，中间包含world_state、同轮turn_context、thread_settings_applied与token_count。3.1.0要求时间完全相同且最多间隔3条记录，因此同次镜像被计成2条。诊断仅定向读取该会话候选的事件元数据，未输出问题/回复正文、未修改源文件；复现和验收全部重新构造隔离合成数据。

解析修复使用同一已知轮次、互补来源、相对首记录0–1秒、仅允许元数据间隔的约束；同源再次出现、不同明确ID、明确触发方式冲突、活动边界或未知/损坏行均保留独立记录。每种来源最多一次，时间窗不滑动，ID别名映射≤10000。已保存事件ID保持稳定，完成通知可补足触发信息。

### v3.1.1 验收

233项Rust测试、44项前端测试、fmt、全目标clippy零警告、前端lint/format/build、release build全部通过；codex-nav/codex-trail均输出3.1.1。合成API增量回归验证先读持久记录、后读镜像时计数仍为1、原ID不变且可补触发信息；同轮次第二次真实压缩保留为2，图、列表摘要与源字节一致。

真实Chrome1848/390px浅深主题验收通过：19ms镜像只剩一个标记，四条独立事件聚合为一个×4并保留各自时间/详情，全部事件入口不残留局部筛选，Escape、键盘、收藏、布局、追加与跨端口收藏持久化回归通过。水平标记上移并用细虚线指回原边，矩形断言和目视确认不覆盖卡片；竖直标记保持居中。QA等前序定位动画稳定后比较视口，避免把动画尾帧误判为按钮副作用。

材料：`/tmp/codex-features-1848-i6Cp6w`、`/tmp/codex-features-390-fuMTpv`。浏览器只用隔离合成数据，测试窗口/服务已关闭。README、CHANGELOG、版本与内嵌资源同步，文档链接和git diff检查通过。本地独立提交并创建v3.1.1标签，不push、不替换用户当前运行服务或全局安装；新版启动后重读旧记录，无需改写会话文件。

## v3.1.0 GitHub 同步 — 2026-09-17

用户明确要求推送，以便其他设备更新。复用上一轮已完成的3.1.0验收；核对远程祖先关系后非强制推送main和新标签v3.1.0，保留所有已有标签。

- [x] 核对本地版本、工作区、远程分支和标签。
- [x] 推送main与v3.1.0，读回远程引用确认。
- [x] 记录同步结果并提供其他设备更新命令。


### 同步验收

推送前工作区仅本轮计划文件发生变化，远程main为a8f566c，是本地HEAD祖先，远程v3.1.0不存在。非强制原子推送main与v3.1.0成功，git ls-remote读回确认两者均指向功能提交e61dbc56e73b41e04ae2e0f75fb47e17beae12c1（标签解引用）。源码未改，复用225项Rust、40项前端及完整浏览器/终端验收；无须重复构建。同步记录作为独立文档提交随main补充，不移动发布标签。其他设备可通过README中的cargo install --git命令更新，停止旧服务后重新启动。

## v3.1.0 — 原画布布局、收藏与会话事件

用户已明确批准修订后的原画布方案并要求实现，无需再次设计确认。保持现有会话侧栏/画布/详情架构，新增横向/纵向与舒适/紧凑切换，会话与问题收藏保存于Navigator本地数据，提交/压缩仅依据可靠记录显示。终端与Codex源文件保持原有只读边界；不调用模型或执行历史工具命令。新功能兼容现有入口，版本升级3.1.0。

- [x] 解析：有界保留提交/压缩事件、命令结果配对与证据约束、未知触发方式、去重/rollback/reset测试，索引输出会话提交摘要。
- [x] 收藏：稳定会话/问题身份、独立持久化存储、原子写入与错误提示、沿用本机认证与同源控制，覆盖重启/异常/API测试。
- [x] 界面：原画布方向/密度、会话及问题星标、提交徽标、压缩连线标记与事件详情，保留当前选择/键盘/拖拽语义。
- [x] 验证：前端状态/布局测试、fmt/clippy/全量Rust测试、前端lint/格式/build、release及合成桌面/窄屏浏览器回归。
- [x] 文档：README、CHANGELOG、版本号、使用边界与知识记录同步，独立本地commit及新v3.1.0标签，不push。

接口核对：Session.events包含kind、关联turn_index、timestamp、source和可选commit/compaction字段；图节点附commits，压缩不新增问题节点，关联不明确或末尾事件在独立列表可见。收藏通过已登记会话key请求，后端转换稳定身份，不持久化s1/q1临时key。浏览器排列偏好可按本机地址保存，收藏由服务端独立保存，不依赖端口。

### v3.1.0 验收与交付

沿用原问题画布，默认纵向舒适；横向依据可用宽度2–4列，紧凑248×96px，切换保留所选问题与真实拓扑。收藏写Navigator独立文件；提交绿、收藏金、压缩紫，小面积语义提示与事件详情分开。未明确记录的压缩触发方式显示未知；仓库为命令工作路径，版本仅匹配有证据的标签。布局偏好按浏览器地址保存，收藏可跨端口。

225项Rust测试、40项前端测试、fmt、clippy全部target零警告、前端lint/format/build、release build通过；两个命令版本均为3.1.0。终端6组交互回归通过。真实Chrome新功能1848/390px通过，原画布1672/1000/390px与管理1848/390px回归通过，覆盖搜索、键盘、拖拽、增量追加、主题、收藏持久化、提交/压缩详情、重命名与回收站操作。所有测试仅操作隔离合成数据，源字节保护通过，自建浏览器窗口与服务已关闭。

窄屏详情按实际剩余画布限制高度并保留150px定位空间；稳定回调和节点测量修复无关渲染导致连线闪失。全套并发测试定位并修复收藏锁被fork继承造成短暂假冲突，以及8条SSE挤占4槽普通请求队列问题；均增加确定性测试。管理验收中gio不支持隔离回收站恢复，脚本将合成文件精确移回原路径并保留trashinfo验证，未改动真实回收站内容。

README、Agent使用说明、CHANGELOG、Cargo版本及内嵌前端资源同步，README浅深截图已更新；本地文档链接检查与git diff检查通过。验收材料：`/tmp/codex-features-1848-GrImMb`、`/tmp/codex-features-390-wDx6Yc`、`/tmp/codex-trail-qa-haeRyb`、`/tmp/codex-management-1848-IBs2wO`、`/tmp/codex-management-390-iG1gCP`。独立本地提交并创建新v3.1.0标签；不push，不覆盖全局安装，构建产物可直接运行。

## 按原问题画布修订预览 — 2026-09-17

用户提供参考截图并纠正方向：保留原有节点连线画布，在此基础上增加横向/紧凑布局和新状态。上一版会话库不是用户认可的方向，保留本地作为已放弃设计。本轮仍只做合成HTML预览，正式功能等待确认。

- [x] 基于原深色点阵、蓝边节点、虚线箭头、小地图与工具栏制作独立画布预览。
- [x] 增加纵向/横向、舒适/紧凑切换，保留问题选择；收藏、提交摘要、压缩事件融入画布。
- [x] 验证拖拽/缩放、布局、收藏与事件详情，检查桌面/手机；记录结果并展示新地址。
- [x] 用户已批准修订后的原画布预览，进入v3.1.0正式功能实现。

设计核对：沿用原深色 #0F172A、卡片 #1E293B、边框 #334155、正文 #E2E8F0、蓝色 #60A5FA；收藏金、提交绿、压缩紫只作小面积语义标记。沿用原14px问题标题、等宽提交信息；紧凑主要压缩空白和增加每行节点数，不依赖缩小文字。主区域始终为原问题画布，右侧详情按需打开；会话栏目收藏保留，问题星标作为画布定位演示。

### 修订预览验收

本地HTML入口 `http://127.0.0.1:8879/`，文件在已忽略的 `docs/design-preview/canvas-refinement/`。复用现有ReactFlow依赖与原canvas.css，未改生产源文件。横向根据视口每行2/3/4个问题，纵向保留原双列蛇形；紧凑116px节点/76px行间距，舒适132px节点/120px间距。会话与问题收藏分开；代码提交在节点上显示绿色徽标，三类压缩在原边上显示，不增加问题节点。合成时间与提交分支/标签保持对应。

独立构建与格式检查通过；真实Chrome 1848/1280/390px各31项交互断言，共93项通过，覆盖原12个问题/11条边、方向密度切换保留选中与关系、收藏不触发详情或移动视口、提交字段、三态压缩、Enter/Escape、最新问题与详情同步、关闭详情恢复视口、缩放、实际鼠标事件拖拽画布/节点、会话收藏、深浅主题及无外部资源。已目视检查1280与390px截图，窄屏保留可读缩放与画布拖动。测试窗口均已关闭，用户预览页保留；截图在 `/tmp/canvas-refinement-*.png`。

原型复核修正详情开关自动回全图、最新问题与旧详情不一致、键盘进入节点等问题；节点按钮双击与节点聚焦隔离。所有数据均为合成，未读取截图中的项目或真实会话。版本、正式README、二进制未变，设计材料仍只保留本地，验收和用户纠正记录独立本地提交，不推送。

## 会话整理与事件可视化 — HTML 设计预览（2026-09-17）

状态：用户不认可此会话库方向，已改为上方“按原问题画布修订预览”；以下仅保留首版过程记录。

本轮仅制作独立 HTML 交互预览，使用合成会话，不连接 Codex、不改正式界面。用户明确要求先看效果，批准后再实现。预览保留于已忽略的 `docs/design-preview/session-library/`，不改变既有设计材料的公开范围。

设计核对：保留蓝白问题画布语言，新增可切换的多列紧凑会话库；收藏与提交分别用星标/金色、提交图标/绿色表达，选中态仍为蓝色。详情按需展开仓库、分支、标签和提交 hash；压缩沿时间顺序显示自动、手动及来源未知三态，无法确定的信息明确留空。先完成设计与浏览器验证，再向用户请求设计确认。

- [x] 制作紧凑/列表切换、搜索/项目筛选、收藏/收藏优先及会话选择的合成预览。
- [x] 展示提交明细、仓库/分支/版本和压缩事件定位；补充暗色与手机布局。
- [x] 浏览器验证交互、持久偏好、空态、键盘与响应式；截图自审，打开可供用户体验的预览。
- [x] 记录验收与设计边界，独立保存本地记录；不发布、不推送。
- [ ] 等待用户批准设计，再另行制定正式功能实现计划。

### 设计 token 与布局

色彩：背景 #F5F7FB、正文 #25334A、主蓝 #3268DA、收藏金 #A86D0B、提交绿 #16735C、压缩紫 #7651B5；白色面板与细边框分隔层级。标题使用本机系统中文无衬线 / Trebuchet MS，正文使用 Noto Sans CJK SC / 系统无衬线，仓库路径和 hash 使用 DejaVu Sans Mono。布局为项目导航 → 多列会话库 → 可关闭的会话概览；小屏改为单列及全屏详情。识别元素是详情内与问题序号对齐的事件轨迹，不增加装饰性统计大卡片。

```text
项目 / 收藏 | 搜索 · 列表 / 紧凑 · 排序       | 当前会话 / 项目路径
            | 卡片   卡片   卡片             | 提交摘要 / 压缩次数
            | 卡片   卡片   卡片             | 问题 → 压缩 → 提交
```

### HTML 预览验收 — 2026-09-17

独立预览含18个合成会话、3个项目；本机地址 `http://127.0.0.1:8878/`。真实Chrome在1848/1280/1000/700/390px下分别通过43/43/44/44/44项断言（共218项），另通过刷新后收藏/主题/收藏优先偏好检查。覆盖多列/列表、分支搜索、项目筛选（含手机入口）、收藏/取消/筛选/优先排序、提交过滤、多仓库、缺失版本标签、自动/手动/未知压缩、事件跳转与问题序号、历史日期、空态、键盘焦点、深浅主题及无外部资源。JavaScript语法、Prettier及diff检查通过；只改独立设计材料与记录，未执行不相关的Rust发布验收。

目视检查桌面深色、手机会话库/概览与事件轨迹截图。复核修正了概览收藏后重建DOM导致焦点丢失、窄屏快捷键落到遮挡搜索框、合成历史日期与事件顺序不一致；手机紧凑模式减少卡片高度。事件定位在展示对话框后直接滚动，避免后台标签页动画帧延迟。验收截图位于 `/tmp/session-library-*-*.png`；全部临时验收窗口已关闭，独立用户预览页保留。

设计文件与说明保留在原本已忽略的 `docs/design-preview/session-library/`，不强制加入公开仓库。本次本地提交只保存验收和知识记录；README正式功能说明、版本号、二进制和真实会话均未变。收藏目前只存预览专属localStorage，真实收藏存储、可靠提交证据和压缩解析仍等待用户批准设计后实施。

## 3.0.0 GitHub 同步

用户本轮要求核对 README 并将最新代码推送 GitHub。复用已完成的 3.0.0 代码和验收，不修改功能或移动既有标签；推送 main 与尚未发布的 v2.1.0、v3.0.0。

- [x] 核对 README 包含正式改名、回收站删除/恢复与 3.0.0 更新说明，确认功能提交与标签一致。
- [x] 核对远程引用，非强制原子推送 main 与两个版本标签。
- [x] 验证远程提交与标签，补充本次同步记录。

### GitHub 同步验收 — 2026-09-11

README 已包含3.0.0更新安装、Codex同步重命名、回收站删除及恢复说明，5项本地链接检查通过。推送前工作区干净，远程main为77f8f7a，确认是本地HEAD祖先；两个目标标签均不存在。git push --atomic 非强制推送main与v2.1.0/v3.0.0成功，git ls-remote读回确认：main及v3.0.0解引用均为4d83b8f4852318cee400a66eb6faa86484642f3a，v2.1.0为73c00c2736c41d38b7e10d48c19514ca1208934c。没有移动旧标签或推送其他引用。

功能源码未变化，复用前一轮192项Rust、31项前端及真实浏览器验收，不重复构建。此次同步记录作为独立文档提交补充，不移动发布标签；没有更新用户全局安装或修改真实会话。

## v3.0.0 — Web 会话整理与 Codex 同步重命名

用户已确认重命名同步 Codex；删除移到系统回收站、可恢复，并在 Web 操作时再次确认。以 Codex app-server 的正式 thread/name/set 管理名称，写前验证 thread/read 的路径与登记目标相同，写后读回确认；不发送模型 turn。删除只对 CODEX_HOME/sessions 内合法登记文件执行 gio trash，不永久删除。此次从纯只读浏览器扩展为显式会话管理，按不兼容安全边界变更升级 3.0.0。开发和验收仅操作隔离合成会话，不对真实会话执行管理动作。

- [x] 明确删除/重命名语义，核对持久化与恢复边界并确定版本。
- [x] 实现管理接口、存储与会话列表/搜索一致性，增加认证、路径校验、失败与持久化测试。
- [x] 增加 Web 操作入口、重命名与删除确认交互、错误反馈与删除后选择行为。
- [x] 全量 Rust/前端检查、release build、桌面与窄屏合成会话验收；同步文档与版本。
- [x] 记录验收和知识，独立本地 commit/tag，不 push。

预审发现：Web 的 s1/s2 key 由登记顺序生成，不能用于持久化名称或隐藏状态；本次直接读取 Codex 的正式名称，不维护另外一份别名。删除后需防止后台扫描/搜索回灌旧记录，并保持已登记 key 不重用。Codex app-server 启动可能初始化自身配置/状态数据库，文档明确只有用户发起改名时才启动管理子进程，浏览不启动。

### v3.0.0 验收 — 2026-09-11

192 项 Rust 测试、31 项前端测试通过；fmt、clippy --all-targets -D warnings、TypeScript strict、ESLint、Prettier、production bundle 与双 binary release 构建通过。7 项管理模块测试验证官方协议、写前/后身份与路径校验、超时/协议异常、相对 CODEX_HOME 和子进程回收；4 项 HTTP 测试覆盖认证、同源、限长、错误参数、名称持久化、回收站失败、外部路径和删除缓存。复核修复了取消未完成索引后其他会话签名未失效导致漏搜的问题，新增确定性回归验证原文件未变也会重新索引。首次回归中的两类失败来自过期帮助文本断言与 fake Codex 参数列表，已同步后复验。

真实 Codex CLI 0.153.2 在隔离合成环境确认 thread/name/set 更新 session_index 与 SQLite 名称，thread/read 重启后仍返回新名称、rollout hash 不变；仅此管理子进程关闭 analytics，不启动模型 turn。真实 Chrome 1848/390px 验收通过：改名/取消/空名、列表/标题/搜索一致、刷新持久化、HTML纯文本、选择/视口保留、删除取消及非当前/当前/最后一个会话。真实 gio 将合成文件移至隔离 XDG_DATA_HOME/Trash，字节 hash 保持；此环境 gio --restore 不能访问隔离回收站，脚本从已核对的回收站副本精确移回原路径，再重启服务验证会话和名称恢复，因此不声称完成系统文件管理器恢复的交互验收。目录 /tmp/codex-management-1848-QIjDDc 与 /tmp/codex-management-390-udopLc，含截图；无真实会话改名或删除。

原问题画布在1848/1000/390px的浅深/系统主题、项目路径、搜索/原文、千问题、小地图、SSE/no-watch、XSS与合成源只读回归通过，目录 /tmp/codex-trail-qa-VRi3XK；README合成截图同步。主代理已目视检查桌面成功页与窄屏删除确认。所有测试窗口和服务均关闭。终端交互未改，未额外执行 terminal_qa.py；全量 Rust 保留现有终端导航/状态回归。

交付 target/release/codex-nav 与 codex-trail 3.0.0，独立本地提交与新标签保存，不 push、不覆盖全局安装。管理只支持 sessions 内文件，回收站当前仅Linux；删除保留Codex名称/数据库记录，不连带其他会话；外部仍运行的Codex进程可能继续持有会话，操作前提示结束该对话。恢复文件后需重启服务清除本次删除状态。文档及AGENTS明确用户授权的写入边界。

## v2.1.0 — 标签页图标与会话项目路径

用户确认需要浏览器标签页 favicon；会话显示记录中的完整项目路径，方便进入对应目录恢复对话。复用蓝色路线品牌标识和现有 cwd 数据，保持现有布局与只读边界。路径缺失时明确提示未记录，不使用 Navigator 的启动目录猜测。

- [x] 增加内嵌 SVG favicon，覆盖主入口与 /trail/ 兼容地址。
- [x] 会话列表与当前会话显示完整项目路径，支持复制并处理剪贴板不可用；补充行为与 API 验收。
- [x] 同步 2.1.0 版本与变更说明，执行格式/lint/全量测试/release build 和桌面、窄屏浏览器验收。
- [x] 记录结果与知识，保存独立本地 commit 和新版本标签，不 push。

设计核对：沿用白/深蓝面板、现有蓝色强调与正文/等宽字体；仅 favicon 使用蓝底白色路线，路径行使用现有次要文字色。完整路径可换行，复制按钮与路径相邻，窄屏无需悬停即可读取。

### v2.1.0 验收 — 2026-09-09

180 项 Rust 测试、30 项前端测试通过；cargo fmt、clippy --all-targets -D warnings、TypeScript strict、ESLint、Prettier、production bundle、双 binary release 与 git diff --check 通过。HTTP 测试覆盖图标 MIME/入口别名/无认证静态资源安全、中文空格路径原样往返、缺失值和列表外显式会话。首次沙箱内测试因禁止本机端口失败，经批准在沙箱外完整复验通过。

真实 Chrome 1848 / 1000 / 390px 验收通过：favicon SVG 请求与解码、完整路径换行不溢出、精确复制、剪贴板拒绝时选中供手动复制、切换缺失路径不残留旧值、列表外 --session 路径正确；原有浅深/系统主题、搜索/详情/画布/小地图、千问题虚拟化、SSE 与 no-watch 手动刷新全部通过。主代理已查看桌面浅深和手机深色截图，README 两张合成截图同步更新，验收目录 /tmp/codex-trail-qa-oJrq2A。脚本创建的浏览器窗口与测试服务均已关闭，真实 Codex 数据未读取或写入。

只读 review 未发现阻断问题；元数据尚未载入时可能短暂提示路径未记录。终端交互未改，本次未额外运行 terminal_qa.py；全量 Rust 测试包含现有终端状态与导航回归。交付 target/release/codex-nav 与 codex-trail 2.1.0，未覆盖全局安装或重启用户现有服务；按本次独立本地提交和 v2.1.0 新标签保存，不 push。

## v2.0.0 — 统一 Web 入口与深色主题

用户要求替换旧网页阅读模式：保留 TUI，`codex-nav --web` 统一打开 Question Trail，`codex-trail` 作为兼容入口。不修改 Codex；本次授权验证后推送 GitHub main 与新版本标签，不强推或重写历史。设计文件与 UI 参考保留本地，不纳入发布。

视觉方案：保留蓝白卡片/曲线布局，深色使用画布 #0f172a、面板 #111827、卡片 #1e293b、边框 #334155、正文 #e2e8f0、强调 #60a5fa；原字体/空间层级不变。跟随系统默认，用户可切换浅色/深色并持久保存，画布、搜索、详情、小地图一致变色，无整页滤镜。

- [x] 统一 CLI/HTTP 静态入口，旧网页资源不再提供；覆盖 session、no-watch、原路径兼容与只读安全测试。
- [x] 实现三态主题、启动无闪白、本地持久化/系统变化、响应式与画布配色；增加自动测试。
- [x] README/Agent 指南同步双模式、安装更新；设计文件精确排除，保留本地副本。
- [x] fmt/clippy/前端格式lint/全量测试/release/浏览器浅深色与终端回归、隔离安装验收。
- [x] 记录结果并提交/tag；核对远程后非强制推送 main 与新版本标签，验证远程提交。

### v2.0.0 验收 — 2026-09-09

179 项 Rust 测试、30 项当前前端测试全部通过（旧阅读页30项测试随旧界面退役，保留现画布23项并新增7项主题测试）；fmt、clippy全目标-D warnings、TypeScript strict、ESLint、Prettier、production bundle、双binary release通过。cargo package --offline --allow-dirty 完整打包并在独立打包源码中编译验证；隔离离线cargo install安装两命令为2.0.0，从/tmp调用版本/doctor通过，未覆盖用户全局安装。

浏览器1848/1000/390px完整回归通过，含浅/深/系统三态、刷新持久保存、不抢所选节点/viewport、完整问题与最终回复、跨会话搜索、千节点虚拟化、小地图/SSE/截断尾行、无外部资源/XSS与合成源字节校验；独立服务验证--session优先非最新目标、--no-watch等待用户刷新才读新增。终端六组交互/恢复回归通过。实际浏览器揭示ReactFlow自身及Background SVG都带默认底色，已用共享变量覆盖并加入SVG精确颜色断言，最终浅深截图经目视检查，保留于/tmp/codex-trail-qa-85uaAA，README只使用合成产品截图。

主入口为codex-nav和codex-nav --web，codex-trail保留同版本兼容，/trail/旧地址可用，旧reader资源404。设计规格/原参考图仍精确忽略；已跟踪设计文档/预览、旧reader源码和旧浏览器脚本仅停止Git跟踪，本地文件全部保留且可从历史恢复，不改写既有远程历史。

发布提交 d4c8a158319c4582c02efc904514601323604372 与新注释标签 v2.0.0 已通过 git push --atomic 非强制推送至 GitHub；git ls-remote 确认远程 main 与标签解引用均指向该提交。未推送其他本地标签。本验收记录以独立文档提交补充，不移动发布标签。

## Question Trail v1.0 / Navigator v1.4.0 — 蓝白问题画布

已完整阅读用户提供的新规格并检查参考图。新入口 `codex-trail` 使用 React Flow + TypeScript，复用 Rust 只读解析和安全本地服务；保留 `codex-nav` 终端与原阅读界面。画布只表达真实顺序及明确的持久化父子关系，不展示隐藏推理，不推断语义分支。默认浅蓝点阵、白色圆角卡片、蓝色选择边框，左侧约 250px 会话栏、右侧约 360px 详情、中央可拖拽缩放图；不复制截图的登录、收藏、回收站等无关功能。

- [x] 数据：验证本机 schema；确定性问题图、分支证据、跨会话搜索、增量 SSE；解析/图/API 自动测试。
- [x] 应用：独立 CLI、内嵌静态资源、默认最新会话、缺失目录诊断；原入口不回归。
- [x] 交互：React Flow 画布、节点/边 hover、选择/聚焦/邻域、搜索跳转、详情/原始 Turn、缩放/小地图/键盘、实时新增提示与历史视口保留。
- [x] 视觉：参考图蓝白密度与卡片样式，桌面/中屏/手机响应式及减少动效；真实浏览器截图对照并迭代。
- [x] 交付：README、版本与变更日志；fmt、clippy、全量测试、前端检查、release、浏览器与终端回归；记录验收，准备独立本地 commit/tag，不 push。

实现边界：推荐的 Node 服务改为复用现有 Rust 服务，减少重复解析、安全及部署逻辑；Node 仅构建时使用。无模型 API、遥测、云服务或 Codex 写入。无法确定分叉点时保留主线并明确解释；不把 fork 记录序号或子代理元数据当作父问题。

### Question Trail v1.0 验收 — 2026-09-08

175 项 Rust 单元/集成测试、53 项前端测试（旧30 + 图18 + 状态5）全部通过；fmt、clippy --all-targets -D warnings、Prettier、ESLint、TypeScript strict、前端 production bundle、双 binary release 和 git diff --check 通过。隔离目录离线 cargo install 成功，从 /tmp 直接调用两命令与 doctor 成功；未覆盖用户全局安装。交付 codex-nav 1.4.0 与 codex-trail 1.0.0。

Chrome 真正视口 1848 / 1000 / 390px 的画布交互全部通过，包括跨会话搜索、前后问题、hover/连线、拖动平移、缩放/fit/全屏、160×92 小地图点击、复制/原文/final、坏行空态、SSE 新增与历史 DOM/视口保留、截断用户行补全恰好一次、XSS/外部资源0、合成源文件字节校验。1000 问题主线首屏仅6个节点 DOM、100%缩放，首次可读约1.25–1.27秒，搜索至Q1000约0.29–0.32秒（含CDP等待，非冷启动保证）。API 合成106.59MiB/1000轮首批约24ms、完整262–276ms（缓存友好，不含浏览器绘制）。旧 Web 实时/手动 × 1280/390px、旧脉络两宽度、终端六组全部通过。

主代理已逐轮查看与参考图对比，修复初始字号/缩放、dagre左右顺序、移动/中屏抽屉遮住节点、DOMRect原型属性展开导致NaN、小地图内部SVG尺寸及点击定位。无分支主线采用两列蛇形及方向箭头保证6张卡可读，不伪造分支；大批live读取与初次loading分开，避免吞掉新增提示。最终截图 /tmp/codex-trail-qa-4ohYYk/，README 使用合成桌面图；仅截图时暂藏已验证的沉浸式翻译扩展浮层，恢复原样，不改应用内容或扩展配置。1672px窗口请求被窗口管理器调整为1848px，按实际宽度报告；非手机真机验收。

只读抽查最近24份本机session的6,683条记录，发现message/event/msg变体、forked_from_id、forked_from_ordinal_exclusive、parent_thread_id、root_turn_id；仅明确parent_turn_id且可唯一向前对应时建立父问题关系。真实网页冒烟发现46个主会话、画布/实时连接成功、外部资源0，不导出内容。新规格与参考原图留本地并精确忽略；测试窗口/服务已关闭，不修改Codex/真实session、不自动push。

## v1.3.0 — 网页问题脉络与交互过程

实现范围：保留阅读视图，增加同会话问题脉络。横向问题路径连接真实轮次，选中问题后纵向浏览已记录的助手消息、工具调用/输出、文件活动和通知，点击查看全文，最终回复独立定位。连线仅表示记录顺序，不推断因果、主题或未公开思维；分页/筛选间隙、省略与未知类型显式展示。不新增云服务、AI API 或 Codex 写入。

视觉方案已与现有 B 阅读室核对：纸白 #f8f7fc、白 #ffffff、深墨 #302a43、紫 #7156ab、浅紫 #ede7f7、告警金 #916a32；沿用中文衬线标题、无衬线正文、等宽序号。唯一重点是“问题轨道 → 选中问题 → 记录步骤”的连线与短促选择动画，尊重 reduced-motion。窄屏轨道横向滚动、详情纵向排布；键盘与可见按钮功能等价。

- [x] 实现并测试纯状态投影：顺序/间隙、记录类型、省略、空态及有界窗口。
- [x] 集成双视图、问题导航、步骤展开、最终回复、分页与稳定实时更新，保持现有阅读功能。
- [x] 合成数据浏览器验收宽窄布局、键盘、搜索/跨页、快速切换、活动窗口与只读隐私。
- [x] 更新 README/CHANGELOG 和 1.3.0 版本，通过 fmt、clippy、前端格式/lint、全量测试、release 构建，记录结果并准备本地 commit/tag，不 push。

### v1.3.0 验收 — 2026-09-08

158 项 Rust 单元/集成测试、30 项前端测试全部通过，cargo fmt、clippy --all-targets -D warnings、Prettier、ESLint、release build、git diff --check 通过。终端六组交互/恢复回归通过；旧 Web 在 1280/390px 的实时与手动刷新模式均通过，新脉络在两种宽度下通过 105 轮/64 活动窗口、搜索间隙、分页焦点、原文 g/G、v 往返、节点复制/最终回复、会话隔离、历史 DOM/内外滚动保留、XSS/无外部请求及源文件 hash 验收。

浏览器复现相关性搜索倒序造成错误连线，已通过可选 order=chronological 在服务端分页前排序修复，并测试默认 relevance 不变；同时修复跨页以旧选择为锚点、内层正文 g/G 和隐藏视图焦点问题。先验证新测试失败，再验证修复。主代理已查看实际桌面/窄屏截图，合成验收目录：/tmp/codex-nav-flow-qa-HwDMeN；旧 Web 目录 /tmp/codex-nav-web-qa-IZ8QGa、/tmp/codex-nav-web-qa-jko8LN。测试窗口与服务均关闭。

交付 target/release/codex-nav 1.3.0，未覆盖用户已安装的命令或运行中的服务。只读边界不变，不修改解析器/真实 Codex 文件，不加依赖、AI API 或持久标注。关系只表达真实记录顺序，不能替代语义因果图或恢复未记录思维；新功能通过独立本地提交与 v1.3.0 标签保存，不推送。

## C 版宣传片 — 补充侧边栏灵感

用户选 C 并要求提及灵感来自 GPT 侧边导航栏跳转历史。按 ChatGPT 侧边栏的历史导航使用体验表达，保留昨天/今天开场，替换重问场景为六秒灵感示意，总长 32 秒；不展示或抓取真实 ChatGPT 历史，不暗示官方关联。沿用 C 配色和字型，侧栏选中条目与正文之间的连接是唯一新增视觉重点。

- [x] 保存原 C 版及制作源码，更新灵感分镜、字幕、发布文案和对比页。
- [x] 仅重导出 C，核对新增场景、音画/字幕时长与旧版可访问性。
- [x] 记录验收并准备独立本地提交，不 push、不改应用版本。

### C 灵感版验收

新增六秒侧栏概念示意，表达用户提供的 ChatGPT 历史导航灵感；C 为 32 秒，保留原反差开场。原 30 秒版本及修改前源码在素材包 `qa/c_original_30s/`，对比页可直接打开旧版；A/B 分镜与成片未改。四项制作检查脚本全部通过，覆盖完整解码、32.022 秒容器时长、音频、七段字幕、入场回归、原稿保留、25 个链接与合成数据 hash。新增场景与最终解码画面人工检查通过，发布文案/旁白/README/对比页同步。未重跑浏览器交互检查或应用构建，未改应用版本、不上传。

## 三版竖屏宣传样片与素材包

面向小红书/抖音，以问题开场，制作 A 痛点反转、B 键盘爽感、C 轻松吐槽三版 9:16 样片。素材放仓库旁独立目录 codex-navigator-promo-20260906，不上传平台、不 push、不修改应用代码。真实界面仅接合成数据，不泄露真实会话；原创动效/音效与清晰字幕，无外部素材版权负担。

- [x] 确认三版脚本、风格与产品事实，准备合成会话和真实界面素材。
- [x] 输出三版可播放 MP4、封面、字幕/旁白稿、发布文案与可再编辑制作源文件。
- [x] 制作本地对比播放页，检查画面/字幕可读性、时长/分辨率/声音与素材隐私，展示效果。

### 宣传素材验收

仓库旁 `codex-navigator-promo-20260906/` 已交付 A 28 秒、B 24 秒、C 30 秒三版 1080×1920 / 30fps MP4，含中文字幕与原创节奏/音效；无人声，另附录制旁白稿。包含六张封面、SRT、平台发布文案、真实界面的合成数据截图、制作源文件、README 和三版对比页。

三版全片解码、视频/音频规格、字幕边界、本地链接、合成会话 hash、入场动画像素回归检查通过；18 张实际解码画面及关键场景抽检通过。浏览器验证三个视频实际播放推进与互斥暂停、选择按钮、1848px 桌面和 390px 手机尺寸窗口布局。修复入场残影与搜索/回复演示连续性后重新导出复验，旧稿保留在素材包 qa 中。未进行平台投放或手机真机验收，未使用真实用户会话，未改应用代码或版本，未重跑应用构建；不 push、不上传平台。详细记录在素材包 `qa/REVIEW.md`。

## 持久 PATH 配置与 Agent 安装交付

用户授权修改本机 Bash 配置，并更新 README 让 Agent 完成持久 PATH 配置。仅最小追加、保留私有备份，不重装程序、不改系统软件、不 push。

- [x] 备份 ~/.bashrc，添加带文件存在检查的 Cargo 环境加载，保留其他配置。
- [x] README 的一句话指令明确授权必要的用户级 PATH 配置；Agent 指南补充备份、幂等修改和新 shell 验收。
- [x] 从不含 Cargo 的初始 PATH 启动新交互式/登录 Bash，在源码外验证命令与版本，记录结果并准备本地提交。

### 持久 PATH 验收

~/.bashrc 已先备份到仓库外私有目录，再追加带存在检查的 Cargo 环境加载。bash -n 通过，程序化逐字对比确认仅增加预期块一次，原配置全部保留。以 /usr/local/bin:/usr/bin:/bin 为初始 PATH，从 /tmp 与用户主目录分别启动新交互式和登录 Bash，均直接解析到已安装的 codex-nav 1.2.0；Cargo 为 1.98.1。嵌套新 Bash 验证 Cargo bin 仅出现一次。

另在源码外、新 Bash 与独立空态数据路径下直接运行 codex-nav，主会话选择入口正常，q 退出成功并恢复终端。验收命令没有提前 source Cargo 环境。README/Agent 指南链接、代码围栏与 git diff --check 通过。此次没有改程序源码、版本或发布标签，未重跑全量构建/回归，不自动 push；用户已经打开的旧终端需重新打开才能应用新配置。

## 已授权历史清理 — 两份根目录文档

用户明确授权清理这两份文档的全部 Git 历史，并更新 GitHub main 与 6 个既有标签。只过滤指定路径，保留仓库外 bundle 和本地文件副本；不删除其他设计/开发文档，不永久删除文件，不清理旧对象或改写 Codex 数据。

- [x] 记录远程引用并验证仓库外完整历史备份，确认工作区无用户未提交改动。
- [x] 移除 README 引用、添加精确忽略规则，重写全部可达提交及标签；逐提交验证其他文件与提交元数据不变。
- [x] 验证本地文档副本不变、历史不再包含目标路径及内容对象、当前文档链接正确。
- [x] 显式逐引用 lease + atomic 推送 main 和 6 个标签，重新克隆验证远程清理；记录结果与恢复边界。

### 历史清理验收

仓库外保留两份完整 bundle（清理前、准备提交后）、两份 Markdown 副本、旧新提交映射和远程 lease 快照。12 个提交逐一验证：除两份文档对应的根 tree 条目和父/tree 引用外，其他文件对象、作者/提交者/时间/消息完全不变；6 个注释标签仅变更目标提交。README 链接检查通过，两份本地文件 SHA-256 与备份一致且已 ignore。

本机 Git 2.25.1 不支持显式 start/commit 指令，首次引用事务被拒绝且未修改任何引用；核对随 Git 安装的手册后，改用受支持的 --stdin 批量旧值校验事务成功。远程采用逐 ref 显式 force-with-lease 与 --atomic 更新 main 和 6 个标签，无额外分支更新；全新 SSH clone 的 12 个提交、6 个标签中均无目标路径及全部 7 个历史文档 blob，fsck 连通性通过。此次只改文档/ignore 与 Git 历史，不发布新功能版本、不重跑程序构建或交互测试。

清理范围是指定分支/标签的可达历史；不清除本地恢复记录、不执行 gc/prune，也不保证旧克隆、fork、托管缓存或隐藏引用自动清除。已有协作者应重新克隆，不能把旧历史合并回公开仓库。验收记录作为普通后续提交同步，不再次移动版本标签。

## 安装 FAQ 与用户文档入口整理

仅修改文档：补充旧 Cargo / PATH 排错，同步 Agent 环境检查；从 README 用户入口移除开发过程链接，保留文件与历史，不修改 ignore、不卸载软件、不自动 push。

- [x] README 补充工具链检查和 FAQ，更新真实仓库入口，精简“更多”链接。
- [x] Agent 指南明确先查现有工具链与 PATH，再决定安装或升级；记录用户反馈。
- [x] 验证排错命令、版本约束、链接与文档差异，记录验收并准备独立提交。

### FAQ 与入口整理验收

本机子 shell 执行 source ~/.cargo/env 后，Cargo 解析到 rustup 安装，Cargo/rustc 均为 1.98.1；最低版本与 Cargo.toml 的 1.88、锁文件 v4 一致。README 仓库地址与 origin 一致，相对链接、代码围栏及 git diff --check 通过。仅修改五份文档，不修改锁文件、软件安装、用户 shell、ignore 或历史；未重跑程序测试/构建，未 push。开发记录仅取消首页链接，仍可在仓库浏览，不宣称已隐藏。

## 安装说明 — 任意目录直接启动

仅澄清现有安装能力，不新增安装器、不修改用户 shell 或全局安装。

- [x] 验证已安装命令脱离源码目录运行，并核对资源是否内嵌。
- [x] README 与 Agent 指南明确 PATH、首次安装和日常启动的区别。
- [x] 检查文档、记录反馈与验证结果，准备独立文档提交。

### 任意目录启动说明验收

在 /tmp 中将此前临时安装的 bin 加入子进程 PATH，command -v、codex-nav --version / --help 均通过；版本为 1.2.0。源码确认 Web 资源 include_str! 内嵌。此检查仅验证脱离源码目录调用，不等同于用户新终端持久 PATH 验收；未修改用户 shell 或全局安装。两份文档的链接、代码围栏与 git diff --check 通过。本次无代码变更，不重跑全量测试、不构建新版本、不 push。

## README 修正 — 终端与网页并列入口

本次仅修正文档，不改程序、版本或标签。

- [x] README 首屏并列两种启动方式，安装指令不再默认只启动 Web。
- [x] Agent 指南补充终端启动/验收/退出，区分两种界面的操作与交付。
- [x] 核对 CLI 与源码、检查文档链接和差异，记录反馈与验收，准备本地文档提交。

### 双入口文档验收

两份指南的相对链接与代码围栏检查通过；启动参数与 release --help 一致，交互式终端要求及 Enter/Tab/f/q/Ctrl+C 与源码一致。3 项 CLI、4 项正文导航测试通过，git diff --check 通过。本次只改文档，未重跑全量测试、终端实机验收或 release 构建，不发布新版本、不 push。

## 用户 README — 一句话交给 Agent 安装自检

本次仅改文档，保留 v1.2.0 二进制与标签，不新增安装器、不发布远程仓库。README 面向使用者，详细步骤移到 Agent 安装指南；缺少公开仓库地址时以用户提供源码目录为入口。

- [x] 精简 README：一句话 Agent 指令、手动启动、核心使用和必要限制。
- [x] 提供可执行的 Agent 安装/测试指南，明确平台、权限、只读、空态和启动验收。
- [x] 检查相对链接及实际命令，记录反馈/验证，准备独立文档 commit，不 push。

### README 验收结果

README 与 Agent 指南的相对链接、代码围栏及 git diff --check 通过；参数与 release binary 的 --help / --version 一致。复跑 157 项 Rust 测试（含 HTTP 集成）、18 项前端测试及 release build 全部通过。本次 shell 未将 Cargo 加入 PATH，使用已安装 Cargo 的绝对路径完成验证，未改动系统配置；指南已覆盖依赖与 PATH 检查。此前本机临时目录安装及脱离源码运行检查通过，本次不重复全局安装、不新增浏览器交互验收。仅提交文档，保留 v1.2.0 与既有标签；公开下载入口仍需仓库所有者发布后提供。

## v1.2.0 — B 专注阅读 Web 正式版

用户已选择 B 并授权推进。实施约定见 docs/web-implementation.md；确认既有 CLI/解析器/只读 worker 后并行实施，最后集成验收，不再停在预览。

- [x] 内嵌资源与 --web / --port / --no-open，保留 TUI 及现有启动参数。
- [x] 实现本机令牌认证、来源校验、分页与有界缓存的只读 API。
- [x] B 主会话入口、搜索、正文/活动/最终回复、复制、历史保护与响应式键盘导航。
- [x] HTTP 安全/状态/增量测试、前端状态测试与真实浏览器宽窄验收。
- [x] fmt、clippy、全量 tests、release、终端回归；同步文档与知识记录。
- [x] 验证只读边界，核对独立 v1.2.0 commit/tag 内容，不 push。

### v1.2.0 验收结果

157 项 Rust 测试（含 11 个 Web 单测、13 个 HTTP 黑盒集成）、18 项前端单测全部通过。fmt/clippy/Prettier/ESLint、release build、六组终端回归通过；终端 harness 修正同尺寸误清屏与 resize 后发键竞争后连续通过。浏览器 1280/390px × 实时/手动刷新四组通过，覆盖 105 轮目录、64 条活动窗口、复制降级、请求失败自动恢复、历史 DOM/分页/滚动保护、精确新增提示与安全渲染。

真实本机 Picker 显示 14 个主会话，主会话 API 正常加载；另显式只读历史子会话 12 轮，文件 SHA-256 不变，不记录内容。已打开正式本机网页供使用。详细结果与边界见 docs/qa.md；交付为独立本地 v1.2.0 提交与版本标签，不执行 push。

## Web 设计预选 — 已选择 B，保留设计阶段记录

范围：只做三套可交互 HTML 预览及本地展示，不实现 --web、HTTP 会话 API 或真实会话接入；现有 binary 与版本不变。使用合成数据，保持主会话入口、生命周期/活动警告分离及最终回复定位语义。

- [x] 写明三套视觉 token、布局和交互范围，确认差异不是仅换色。
- [x] 制作 A 清晰工作台、B 专注阅读、C 紧凑控制台，以及统一切换展示页。
- [x] 浏览器验证会话选择、搜索、时间线、最终回复、折叠活动、宽窄布局和键盘；核对无外部资源/真实会话请求。
- [x] 记录预览验收、打开本地展示页并准备独立设计预览提交；不发布新 binary 版本。
- [x] 用户已选择 B，进入正式 Web 实施计划。

### 设计预览验收

四份 HTML 内联 JavaScript 语法与无外部依赖检查通过。三套样稿在约 1280 / 390px 外框宽度下各 18 项浏览器断言通过，覆盖默认会话选择、搜索空态、选择、f/g/G、折叠、帮助、返回与无横向溢出；A 的模拟追加另检查历史保护和无 final 提示。已查看桌面三方案和手机截图，修正 A 的按钮焦点快捷键及选中轮次可见性。

原有 132 tests、fmt、clippy 与 release build 通过；Rust 源码与 binary 版本未修改。预览服务只绑定 127.0.0.1:8877，仅提供 docs/design-preview。后台 QA tab 已关闭，按用户展示需求保留新开的预览窗口；未操作已有页面。正式 Web 开发等待用户选择，不创建发布标签。

## v1.1.2 — 轮次生命周期与活动警告分离

正常完成只描述执行生命周期，不判断答案正确。工具/命令错误保留 activity.errors，Timeline 独立显示 !N；显式轮次错误显示 ✕，中断显示 ⊘，完成显示 ✓。未知/未完成/rollback 语义保留。Web 仅评估当前架构，不新增服务或网络监听。

- [x] 补充失败后重试再完成、未结束活动错误、明确执行错误、中断、晚到工具记录与 rollback 的 parser/state 测试。
- [x] 分离状态判定及错误计数，更新 Timeline、正文摘要和帮助，确保不宣称答案正确。
- [x] 验证宽窄终端、f/g/G、完整测试及 release；同步使用文档、Web 支持边界、经验和版本。
- [x] 记录验收并准备本地 v1.1.2 commit/tag，不推送。

### v1.1.2 验收结果

旧实现先复现“活动失败后正常 completion 仍为 Failed”；修复后新增 6 项 parser/state 和 4 项 UI 回归通过。全量 132 tests、fmt、clippy 零 warning、release build、6 组真实终端验收通过。只读检查本机 12 份 rollout 的事件类型，不记录内容；合成终端测试文件 hash 不变。Web 仅完成架构评估与文档，未实现或启动网络服务。

## v1.1.1 — 默认主会话选择入口

按最新反馈修正入口：不传 --session 时始终进入 Picker，只列明确识别为 MAIN 的会话；子代理与 UNKNOWN 不进入列表。--all 仅扩展日期范围；显式 --session 仍可直接打开任意类型，不重定向父会话。保留 cwd 相关度与更新时间排序。

- [x] 补充主会话过滤、显式打开非主会话、单一主会话不自动打开和返回/刷新 Picker 回归。
- [x] 移除自动打开路径，统一启动与返回列表的主会话过滤，更新空态和 CLI 说明。
- [x] 同步文档、经验、版本；通过 fmt、clippy、全量 tests、release 和实际终端验收。
- [x] 记录验收结果并核对本地 v1.1.1 提交内容与标签命名，不执行 push。

### v1.1.1 验收结果

122 tests 全部通过，fmt、clippy 零 warning、release build 通过。旧 release 已由真实终端测试复现唯一 MAIN 自动打开，新版终端 5 组验收通过，覆盖启动/Enter/s/r/搜索/--all、非主会话空态、显式子会话 ID/path 和原有 f/g/G/终端恢复。所有合成文件 hash 不变，不修改 Codex 数据。交付版本为 1.1.1，以独立本地 commit/tag 保存。

已完整阅读产品规格及使用指南。执行采用规格指定的 Rust 独立 TUI，不修改 Codex，不包装 Codex PTY，不写入 CODEX_HOME；无需阶段性确认。

- [x] Phase 0：检查工具链与真实会话结构，建立 crate 和兼容性记录，构建成功。
- [x] Phase 1：实现有界流式解析、领域模型、去重、状态与 rollback；通过 parser 自动测试。
- [x] Phase 2：实现近期发现、索引辅助、目录排序、配置和 doctor；通过 discovery 自动测试。
- [x] Phase 3：实现 Picker、Timeline、Viewer、搜索、响应式布局、键盘与安全终端恢复；通过状态及渲染测试。
- [x] Phase 4：实现后台增量读取、文件替换恢复、实时跟随与历史选择保护；通过增量测试。
- [x] Phase 5：验证大文件、Unicode、损坏记录、剪贴板降级及真实本机会话。
- [x] Phase 6：完成 README、架构文档、LICENSE、CHANGELOG 和发布构建。
- [x] Phase 7：运行 fmt、clippy、全部测试、release 及终端集成验收，记录结果。

## 设计约定

- 流式读取使用 File::open 和有界缓冲；后台线程分批更新，TUI 不执行全文件阻塞解析。
- 正常文本保存在有总量上限的 normalized model；超大记录跳过并累计诊断。
- 兼容 event_msg、response_item、task_started/task_complete、item_completed PascalCase 类型；优先 content_item_kinds 识别用户输入。
- 搜索索引随 Turn 变更增量更新。session_index 仅补充标题，真实 rollout 为准。
- 所有 fixtures 人工合成，不复制真实 Prompt、工具输出或认证数据。
- 核实计划与规格一致后直接实施；只有真实阻塞才暂停。

## 验收结果

全部 7 个阶段已完成。91 项自动测试通过；fmt、clippy（零 warning）、release 构建和伪终端交互验收通过。真实历史文件 hash 保持一致，真实活动会话观察到增量追加，无 reset；没有向 Codex 写入任何数据。

发布文件：target/release/codex-nav（1.0.0）。文档：README.md、docs/architecture.md、docs/session-format-notes.md、docs/qa.md。详细测试与平台限制记录在 docs/qa.md。

## v1.0.1 — 本地版本历史与正文首尾跳转

需求已确认：g/G 按焦点生效；时间线继续选择第一轮/最新轮，正文跳到当前轮顶部/底部，宽窄布局切换后行为一致。建立本地 main 分支，以独立 commit 和版本标签保留迭代历史。

- [x] 初始化 Git，提交现有 v1.0.0 基线并建立标签。
- [x] 补充宽窄切换与正文 g/G 回归测试，再修复按键分发。
- [x] 更新帮助、使用文档、CHANGELOG 和版本号至 1.0.1。
- [x] 通过 fmt、clippy、全量测试、release 与终端交互验收。
- [x] 记录验收与经验，核对 v1.0.1 提交内容和版本标签命名。

### 本次验收结果

基线 commit e9fd58a，标签 v1.0.0。新增测试先在旧实现上复现 g/G 错误切换 Turn，修复后 4 个导航回归测试全部通过。全量 95 tests、fmt、clippy、release 构建和真实伪终端宽窄切换验收均通过。

正文 g/G 只改变当前正文滚动位置，保留所选 Turn 和新增消息提示；时间线 g/G 语义不变。版本号、帮助、README、使用指南、CHANGELOG 与本地迭代约定均已同步。交付通过独立修复 commit 和 v1.0.1 标签记录，提交历史可用 git log --graph --decorate --oneline --all 查看。

## v1.1.0 — 最新输入保护、会话辨认、最终回复定位

三个已授权需求作为同一可验收版本推进；只读取真实 Codex 文件，所有写入与测试数据留在 Navigator 仓库/测试目录。

- [x] 建立内存耗尽回归：历史输出不能挤占 Prompt；连续大量 Prompt 后最新输入仍可见、可搜索，内存保持有界。
- [x] 将保留文本拆成 Prompt 与活动两个滚动预算；淘汰旧正文时保留时间线摘要和明确省略提示，增量更新搜索。
- [x] 按真实 metadata 识别主会话、子会话、未知来源，显示父会话/代理标识及更新时间，排序优先相关主会话，监控状态不暗示模型正在工作。
- [x] 使用 f 定位当前轮最后一条可靠最终回复；标注 FINAL ANSWER，没有明确结果时提示，不猜测，不切换 Turn。
- [x] 覆盖 parser/discovery/state、宽窄渲染、增量 worker 和实际终端交互；验证真实本机会话的识别信息，不记录敏感内容。
- [x] 更新 README、CHANGELOG、版本及验收记录；通过 fmt、clippy、全量 tests、release build 后准备本地提交和 v1.1.0 标签。

设计边界：每条 Prompt 仍保留 256 KiB 上限，Prompt 总预算 16 MiB，活动总预算 48 MiB；超限时让位给新内容。更早的 Prompt 保留预览，历史全文省略必须有明确标识。最终回复定位只针对已保留且有可靠标记的消息。暂不加入全文分页、磁盘缓存或跨会话正文搜索。

### 本次验收结果

新增 26 项测试，全量 121 tests 通过；fmt、clippy 零 warning、release build、终端宽窄导航和恢复验收通过。内存测试覆盖 49 MiB 工具压力、17.5 MiB Prompt、48 MiB 精确预算边界、长中文回复 phase 升级、后台 dirty 淘汰同步。真实主/子会话身份正确，分别检测到 9 / 4 条最终回复，历史文件 hash 不变。详细结果见 docs/qa.md；交付为 codex-nav 1.1.0，本地独立提交与 v1.1.0 标签，不执行 push。
